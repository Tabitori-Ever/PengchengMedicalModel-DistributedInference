# 版本记录

> 本文档记录 PengchengMedicalModel-DistributedInference 平台的版本演进。
> 版本号 = 镜像 tag / 代码里程碑；`*` 表示重要更新。
> 历史格式沿用 AlexNet-K8s 的 `### vX[.Y] - 标题` + 特性表风格。

## 版本演进图

```
v1.0 多模型边云协同平台搭建*
  ↓
v1.1 镜像瘦身与多阶段构建
  ↓
v1.2 推理服务稳定性修复（连接池/重试/指标）
  ↓
v1.3 任务生命周期完善 + 前端静态托管
  ↓
v1.4 调度与监控解耦（prediction/monitoring 服务）
  ↓
v1.5 调度缺陷修复（实体路由分析/双队列机制）
  ↓
v2.0 医院/诊所 Pod 化架构 + 新前端 + 集群编辑器*
  ↓
v2.0.1 补丁：clinic 用量解析、/health part2 探测
  ↓
v2.0.2 补丁：内置患者数据集、前端 AlexNet 请求结构、新增 clinic 镜像兜底
  ↓
v2.0.3 补丁：节点负载 metrics-server 实时化、提交结果内联详情
  ↓
v2.0.4/v2.0.5 补丁：任务 input 瘦身(性能根治) + /cluster/summary +
               AlexNet 内置轻量样本集 + part2 置信度归一化
  ↓
v2.0.6 补丁：集群编辑器点击切换详情、clinic Pod 可删除(重置恢复)
  ↓
v3.0 正式版：云/边/端架构 + 四类任务(诊断/计算/通信/日常)*
      移除 AlexNet(part1/part2)，调度功能由数据中心继承   ← 当前版本
```

---

---

---

---

---

---

---

---

---

---

---

### v3.0.12 - 性能：轮询瘦身 + 服务端缓存 + 静态资源长缓存（外部设备加载慢修复）

| 问题 | 外部设备访问缓慢/加载不出：/cluster/status≈3.6s、/cluster/summary≈5.6s 且**每请求都实时查 k8s+metrics**；架构页 5s 轮询 5 个接口 × 多设备多标签；静态资源无 Cache-Control 每次回源校验；node3 上行带宽有限 |
| 服务端缓存 | `/cluster/status` 与 `/cluster/summary` 增加 4s TTL 缓存（环境变量 `CLUSTER_CACHE_TTL` 可调）；节点负载复用 `node_usage` 3s 缓存，减少重复 metrics/k8s 调用 |
| 轻量轮询 | 新增 `GET /tasks/recent?limit=N`：仅返回最新任务的精简结构（id/状态/阶段/耗时 + 轨迹所需的小字段，去掉大体积 result/input），供实时架构与轨迹轮询 |
| 静态缓存 | `/app/assets/*` 返回 `Cache-Control: public, max-age=31536000, immutable`（哈希文件名），`/app/*` 入口 `no-cache` |
| 前端降频 | 实时架构页轮询 5s→10s 并改用 `/tasks/recent`；`useTicker` 在标签页隐藏时自动暂停轮询，避免后台标签/多设备叠加压垮单进程调度器 |

| 文件 | 变更内容 |
|---|---|
| `scheduler/api.py` | `/tasks/recent` 精简接口 + 静态资源缓存头中间件 |
| `scheduler/cluster_api.py` | status/summary TTL 缓存；节点负载复用 node_usage 缓存 |
| `frontend/src/api.ts`、`pages/ArchitecturePage.tsx`、`hooks.ts` | 轻量接口、10s 轮询、隐藏暂停 |

镜像：`inference-scheduler:v3.0.12`。

---
### v3.1.2 - 前端：用户平台交互修复与极简 + 数据集选择 + 并发栏可见性

| 特性 | 修复/说明 |
|---|---|
| 点击任务类型卡住 | 根因：草稿表单只挂在已提交的消息上，消息列表为空时表单永不渲染 → 引入 `pendingId` 草稿消息模型：`pickKind` 立即追加一条无 taskId 的用户气泡并在其中渲染表单，再次点击其它类型就地改写不新增气泡，`submitTransition` 提交后同一气泡补上 taskId 且只追加助手气泡；欢迎态在有草稿时隐藏 |
| 结果极简 | 新增 `src/user/UserResult.tsx`：只给结论（诊断 bpCR 概率大字 + pCR/non-pCR + 患者；计算 N 个分区计算完成 · 数据量；通信 拉取/上传/已生成备份；日常 N 个任务已完成），运行中只有「正在执行/排队中 + 进度条」，失败给原因；不再显示时延、阶段明细、执行节点与指标表 |
| 文案清理 | 移除头部副标题、「四类任务均在本页发起并实时反馈，无需跳转。」、品牌副标题（不再使用 `PLATFORM_TAGLINE`）、助手气泡底部「在管理平台查看 →」 |
| 数据集选择 | 新增 `frontend/public/datasets/`（`index.json` + 计算 3/通信 3/日常 1/诊断 1，共 8 条；诊断 10 位患者 ID 取自 `test/test_dataset.json`），构建后发布到 `/app/datasets/`；点「选择数据集」按当前类型筛选列出（名称/说明/大小），选中即按内容预填参数并可「清除」；保留「从本机选择文件」（JSON 预填，其它格式仅记录文件名与大小，文件体不上传） |
| 参数极简 | 全站去掉优先级；诊断固定发起方 **医院 1 / clinic-1** 自动转诊，仅选患者；计算选 小(2/128/20/2)·中(4/256/40/3)·大(8/1024/120/6) 规模预设；通信选 普通(20/4/4)·省流(8/2/4)·高速(60/8/16) 方式预设；日常仅任务数；其余进「高级参数」（默认收起） |
| 并发栏可见 | 根因：并发窗口按当前时钟取「最近 10 分钟」，库中历史任务全在窗口外 → `buildLanes` 改为始终展示最近任务（≤12 条，按 start_time 倒序取、正序展示），窗口起点取 min(10 分钟前, 最早任务起点)，全部任务早于 10 分钟且无进行中任务时退化为「最近任务区间」并标注「最近任务（无进行中任务）」；该区块由地图下方移到**任务选择/信息条之后、轨迹地图之前** |
| 去跨平台入口 | 用户平台内不再有任何跨平台/回首页链接；管理平台侧栏删除「前往 / 用户平台 / 首页」整块 |
| 命名清理 | 轨迹图内残留的「云端*」「发起端」统一为「数据中心*」「发起方」 |

镜像：`inference-scheduler:v3.1.2`（hospital/clinic/dc 仍为 v3.0，无后端改动）。

验证：`npm run build` 干净（113 modules，三入口产物 + `dist/datasets/`）；线上实测 `/app/`、`/app/user/`、`/app/admin/`、`/app/datasets/*` 均 200 且为新产物（`user-DU_zH8_9.js` / `admin-WpHGJEI9.js` / `index-C9OFUFTY.css`）；用户产物内已无「时延/阶段/node1/node3/执行者/优先级」字样；四类任务经调度入口实测全部 `finished`（诊断 9.5s、计算 0.16s、通信 14.6s、日常 16.6s，均由 clinic-1 发起）。

---
### v3.1.1 - 前端：管理平台新增「平台总览」

| 特性 | 说明 |
|---|---|
| 平台总览 | 管理平台新增 01「平台总览」：三级架构与四类任务说明、节点就绪/业务实体/运行实例/任务总数/运行中·队列/组件健康 KPI、任务类型分布与功能入口；不含负载与结果细节 |

镜像：`inference-scheduler:v3.1.1`。

---

### v3.1.0 - 前端：三级命名体系（数据中心/医疗中心/医院）+ 用户平台与管理平台拆分

| 特性 | 说明 |
|---|---|
| 命名体系 | 全域统一为 数据中心（云）→ 医疗中心（原 hospital，边）→ 医院（原 clinic，端）；实体显示为 医疗中心 A/B、医院 1/2…；术语集中到 `src/terms.ts` 单一来源，便于后续再改 |
| 双站点 | 多入口构建：`/app/` 首页选择入口、`/app/user/` 用户调用平台、`/app/admin/` 管理平台 |
| 用户平台 | 类 ChatGPT 简洁对话式：四类任务卡片 → 表单气泡（含可选数据文件上传/预填）→ 助手气泡实时显示阶段/进度 → 完成后内联结果；左侧历史任务列表 |
| 管理平台 | 导航：可视化 / 综合测试 / 集群负载 / 集群编排；可视化保留逻辑架构总览（去负载）+ 任务执行轨迹 + 任务总线；集群负载集中节点资源/Pod 就绪/组件健康/任务队列 |
| 风格简化 | 正文 16px、表格 15px、标题 32px、导航 16.5px；每页只显示本页信息；进一步去除冗余英文与说明 |
| 轨迹命名统一 | 轨迹图层标签改为 `数据中心 / 医疗中心 / 医院`；实体显示 医疗中心 A/B、医院 1/2…；清理 边/端/诊所/边缘节点 等旧词与冗余英文 |
| 多任务并发栏 | 可视化页轨迹卡片新增「多任务并发 · 调度态势」：时间窗内多任务并发条形（颜色=任务类型，形状=来源层级，运行中实心脉冲/失败锈红）+ 调度队列按优先级排序（标注并发上限 4）；点击行可切换被追踪任务 |
| 播放与过渡 | 自动播放加快至 2s/步（末步 2.4s，钳制 1–3.5s），新增 350–450ms 过渡避免状态突变；保留 reduced-motion 关闭动效 |


镜像：`inference-scheduler:v3.1.0`。

---
### v3.0.11 - 前端：轨迹图与架构图去英文精简 + 节点资源集中展示 + 转诊单线 + 侧栏中文化放大

| 特性 | 说明 |
|---|---|
| 轨迹图精简 | 去除 `TASK TRAJECTORY MAP / STEP TIMELINE / DATA CENTER / AUTO / MANUAL / LIVE / LOOP` 等英文；层标签改纯中文 `云数据中心 / 医院 · 边 / 诊所 · 端`；不再显示“医院 Pod · EDGE / 诊所 · 端”等角色说明，仅保留名称+能力简述 |
| 节点资源集中 | 架构总览移除每个实体 tile 内重复的 CPU/内存 数值与条，改为独立小框「节点资源」集中展示 desktop/node1/node2/node3 的 CPU%、内存% 与细条 |
| 转诊单线 | 删除逐对 clinic→hospital 连线，改为「诊所 · 端」与「医院 · 边」面板之间的一条点线，标签 `转诊`，消除多条线与错位 |
| 侧栏中文化 | 左侧边栏移除全部英文注记（品牌副标题、导航英文小字、页脚），品牌副标题改中文；导航 16px、品牌 18–19px 放大 |
| 架构图精简 | 卡片标题去掉 `LIVE CLUSTER OVERVIEW` 等英文，仅保留「当前集群架构总览」 |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/TaskTrajectoryMap.tsx` | 英文与角色说明清理、层标签中文化 |
| `frontend/src/components/ClusterOverviewMap.tsx` | 节点资源集中小框、转诊单线、去英文标题 |
| `frontend/src/components/Layout.tsx` | 侧栏去英文并放大文字 |
| `frontend/src/styles/index.css` | 上述样式调整（追加） |

镜像：`inference-scheduler:v3.0.11`。

---
### v3.0.10 - 前端：架构总览精简统一 + OPS 重叠修复 + GET 失败自动重试

| 特性 | 说明 |
|---|---|
| 去冗余 | 架构总览去除英文层名(CLOUD/EDGE/TERMINAL/OPS)与编号(01–04)、`LOGICAL VIEW` 英文注记与重复描述；实体仅保留 名称 + 极简中文说明 + 实时状态/负载 |
| 语言统一 | 统一为中文：`云数据中心` / `医院 · 边` / `诊所 · 端` / `运维`；连线标签精简为「协同诊断 / 计算 / 同步 / 日常」「协同计算 / 同步 / 日常」「转诊」「指标观测」 |
| OPS 面板 | 修复右侧“运维”面板文本重叠/裁切，面板宽度与排版重排，整体层级更简洁直观 |
| 抖动韧性 | 前端 GET 请求在超时/网络错误时自动重试一次（间隔 0.8s），缓解 kubelet/网络瞬时抖动导致的页面超时 |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/ClusterOverviewMap.tsx` | 精简与语言统一、OPS 布局修复 |
| `frontend/src/styles/index.css` | 逻辑视图样式调整（追加） |
| `frontend/src/api.ts` | GET 自动重试拦截器 |

镜像：`inference-scheduler:v3.0.10`。

---
### v3.0.9 - 前端：集群架构总览改为逻辑架构视图

| 特性 | 说明 |
|---|---|
| 逻辑分组 | 由“4 张物理机卡片”改为按逻辑分组面板：`01 CLOUD · 云 Data Center` / `02 EDGE · 边 hospital` / `03 TERMINAL · 端 clinic` / `04 OPS · 运维 desktop`（自下而上：端→边→云，运维在侧） |
| 实体绑定 | 组内为无边框图标实体（scheduler/medical-server/dc-services/redis、hospital-a/b@node1/2、clinic-1/2@node1/2、monitoring/prediction），显示实时健康状态与所在节点 CPU/内存数值+细条 |
| 关系连线 | 边↔云 `协同诊断 / 协同计算 / 数据同步 / 日常任务`；端↔云 `协同计算 / 数据同步 / 日常任务`；clinic-1⇢hospital-a、clinic-2⇢hospital-b 点线 `医疗推理转诊`；运维⇢云 点线 `Prometheus/metrics-server 观测`；云内 scheduler—medical-server / dc-services / redis 连接 |
| 视图标识 | 卡片标注 `逻辑架构视图 · LOGICAL VIEW`，节点信息以 `@node1` 形式附在实体上 |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/ClusterOverviewMap.tsx` | 重写为逻辑架构视图（分组面板 + 实体 + 关系连线） |
| `frontend/src/pages/ArchitecturePage.tsx` | 传入 health 供组件绑定状态 |
| `frontend/src/styles/index.css` | 逻辑视图样式（追加） |

镜像：`inference-scheduler:v3.0.9`。

---
### v3.0.8 - 前端：并行步骤同步高亮 + 集群架构总览图 + 重点信息强化

| 特性 | 说明 |
|---|---|
| 已执行灰化 | passed 连线/节点改为中性灰，仅“当前”步保持橙色高亮+脉冲+流动光点；图例改为 已执行(灰)/当前(橙)/待执行(淡灰虚)/无关(最淡) |
| 并行步骤 | 分区计算/ P2P 拉取 / Job 执行建模为并行步骤(parallel group)：激活时其全部连线与节点同时高亮；时序芯片如 `④ 分区计算 ∥3 · 3 项`，耗时取子项最大值 |
| 任务下拉 | 任务选择改为 `<select>` 下拉，选项以任务 ID **前缀**优先展示 + 状态/发起端/耗时；保留“自动跟随最新” |
| 架构总览图 | 轨迹地图**上方**新增 `当前集群架构总览 · LIVE CLUSTER OVERVIEW`：4 节点卡片(控制面/边×2/云)带角色、Ready、CPU/内存实时数值与条、承载 Pod 图标与数量、节点间关系连线，5s 实时刷新 |
| 减少方框 | 轨迹信息条由“每字段一方框”改为无边框层次化排版：一级信息(状态/当前步骤/耗时/关键结果)加粗加深，二级信息(任务ID/发起端/节点)小灰标签+中灰值，以细分隔线代替框线 |
| 对比度强化 | 全站次级文字加深、关键数值加粗墨色、卡片内层框线减少，重点一眼可见 |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/TaskTrajectoryMap.tsx` | 灰化策略、并行步骤模型、任务下拉、信息条层次化 |
| `frontend/src/components/ClusterOverviewMap.tsx` | 新增当前集群架构总览图 |
| `frontend/src/pages/ArchitecturePage.tsx` | 增加 clusterStatus 轮询并置于轨迹上方 |
| `frontend/src/styles/index.css` | 地图/总览样式 + 对比度与重点强化覆盖层 |

镜像：`inference-scheduler:v3.0.8`。

---
### v3.0.7 - 前端：轨迹地图手动逐步/慢速循环 + 图标节点 + 数据中心整体面板 + 全站层次与字号提升

| 特性 | 说明 |
|---|---|
| 手动逐步 | 轨迹时间轴新增 `⏮ 重置 / ◀ 上一步 / 下一步 ▶`：点按一次只推进/回退一步（末端不回绕），手动操作立即接管自动播放；AUTO/MANUAL/LIVE 模式标识 |
| 慢速循环 | 自动模式改为 **2.5s/步**（当前步带进度条）+ 末步停留 3s → **回到 ① 循环演示**，并显示 `循环演示 · LOOP` 标识 |
| 图标节点 | 节点方框改为**图标徽章**（自绘内联 SVG）：scheduler 齿轮 / medical-server 机架 / dc-services 数据库 / redis 缓存堆叠 / hospital 医院十字 / clinic 听诊器 / Job 立方体；四态配色沿用 |
| 数据中心整体 | 云层四个组件被包裹进 `DATA CENTER · 云数据中心` 面板（黄铜描边+底色+虚线内规+组件/运行中/排队实时摘要），当前步触及云组件时整体高亮 |
| 层次与可读性 | 正文字号 13→15px、表格 12.5→14.5px、按钮/徽章/表单同步上调；页面标题 30px 且带分隔线；卡片/小节标题加黄铜前导线；KPI 数值 42px；侧栏与导航放大；表格行高与 hover 加强；焦点可见 |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/TaskTrajectoryMap.tsx` | 控制逻辑(nextPlayState)、图标节点、DC 容器、慢速循环进度 |
| `frontend/src/styles/index.css` | 地图样式追加 + 全站 TYPOGRAPHY & HIERARCHY 覆盖层 |

镜像：`inference-scheduler:v3.0.7`。

---
### v3.0.6 - 前端：轨迹地图按时间顺序高亮 + 其余灰化

| 特性 | 说明 |
|---|---|
| 顺序高亮 | 轨迹地图改为按执行时间顺序（提交→调度→转诊/分区/拉取/Job→聚合/备份/日志→结果回传）逐步高亮节点与连线，不再一次性全部高亮 |
| 状态分级 | 四态渲染：已执行(黄铜) / 当前(橙色脉冲+流动光点) / 待执行(极淡灰虚线) / 无关(最淡灰)，无关节点与连线显著灰化 |
| 时间轴播放 | 地图下方新增步骤时间轴（①提交·12ms ②调度 …），可点击任一步跳转；完成任务默认按序自动播放一次(约 0.9s/步)，可 ▶ 重播/⏸ 暂停；运行中任务按 stage 定位当前步；失败任务停在失败步(锈红) |
| 减少动效 | 遵循 prefers-reduced-motion：关闭自动播放与虚线动画，仍保留四态区分 |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/TaskTrajectoryMap.tsx` | 步骤时间轴推导 + 光标/自动播放 + 四态渲染 |
| `frontend/src/styles/index.css` | 时间轴与四态样式（追加） |

镜像：`inference-scheduler:v3.0.6`。

---
### v3.0.5 - 前端：实时任务执行轨迹地图（云/边/端三层动态连线）

| 特性 | 说明 |
|---|---|
| 轨迹地图 | 实时架构页顶部新增「任务执行轨迹 · TASK TRAJECTORY MAP」：上中下=云 CLOUD / 边 EDGE / 端 TERMINAL 三层，节点间按实际执行关系连线 |
| 四类可选 | 诊断/计算/通信/日常 四个 Tab 切换轨迹模板；每个模板按既有 mermaid 执行流程绘制（转诊→worker→server；分区协同→聚合；P2P 拉取→上传→备份；Job 创建→日志→删除） |
| 动态高亮 | 依据真实任务数据高亮实际参与节点与连线（诊断的转诊医院、计算的分区 actor、通信的 peers、日常的 Job 节点）；运行中任务按 stage 高亮当前阶段，连线使用流动虚线动画与节点脉冲 |
| 任务选择 | 自动跟随该类型最新任务（可关闭/manual），列出最近 6 条任务可选；信息条显示 ID/状态/发起端/阶段/耗时与类型关键事实；无任务时显示标准执行路径 |
| 数据来源 | 复用 /tasks、/cluster/summary、/tasks/stats、/health，无后端协议变更 |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/TaskTrajectoryMap.tsx` | 新增轨迹地图组件（SVG 三层布局 + 边/节点高亮 + 动画） |
| `frontend/src/pages/ArchitecturePage.tsx` | 轮询 /tasks 并置于顶部渲染轨迹地图 |
| `frontend/src/styles/index.css` | 轨迹地图样式与连线动画 keyframes |

镜像：`inference-scheduler:v3.0.5`。

---
### v3.0.4 - 前端：集群列表视图 + 实时架构总览 + 科研档案风改版

| 特性 | 说明 |
|---|---|
| 集群列表视图 | 集群编排页新增「地图视图 / 列表视图」切换：按节点分组显示节点角色/负载/就绪与业务 Pod 摘要表（实体/类型/副本/亲和/镜像/实例/操作），列表行支持拖拽到其它边缘节点改 node（与地图拖动一致），保留右侧 Pod 详情面板与校验/应用/重置 |
| 实时架构总览 | 新增导航「02 实时架构」页：按 云 Data Center / 边 hospital / 端 clinic 三层实时展示组件健康（/health）、节点负载（/cluster/summary）、任务总线（/tasks/stats）与四类任务流程条，5s 自动刷新 + 手动刷新 |
| 设计语言 | 参考 RhineLabUI 的科研档案终端风格：纸灰底/墨黑/黄铜点缀、细线边框、大写字距微标签、等宽数字、编号导航、克制阴影；全站（总览/提交/记录/综合测试/集群/结果）统一样式 |
| 数据来源 | 复用现有 /cluster/status、/cluster/summary、/tasks/stats、/health，无后端协议变更 |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/pages/ArchitecturePage.tsx` | 新增实时架构总览页 |
| `frontend/src/components/ClusterList.tsx` | 新增集群列表视图（含 HTML5 拖拽改 node） |
| `frontend/src/pages/ClusterPage.tsx` | 视图切换与两视图共用工具栏/侧栏 |
| `frontend/src/styles/index.css` 等 | 全站样式改版 + 新组件样式 |
| `frontend/src/api.ts`、`types.ts` | 新增 health 接口与相关类型 |

镜像：`inference-scheduler:v3.0.4`（hospital/clinic/dc 仍为 v3.0）。

---
### v3.0.3 - 补丁：分阶段详细时延统计（默认汇总 + 展开明细）

| 特性 | 说明 |
|---|---|
| 详细时延 | 恢复各阶段细分计时：诊断=队列/Worker(计算·网络)/阶段间/Server(计算·网络)/流水线/端到端；计算=每分区耗时与 CPU；通信=P2P 拉取/云上传/云备份；日常=每个 Job 墙钟与 CPU |
| 默认汇总 | 结果卡片默认显示“时延统计”汇总（端到端/流水线/队列等待/类型要点） |
| 展开明细 | 点击「▸ 展开详细时延」展示分阶段瀑布明细（下箭头收起） |
| 计时键 | result.metrics 统一含 queue_wait_ms/pipeline_total_ms/e2e_total_ms 及各类型细分键 |

| 文件 | 变更内容 |
|---|---|
| `scheduler/scheduler.py` | _finish 增加 queue_wait/extra_metrics；诊断 worker/server 计算-网络拆解与阶段间开销；同步测量上传/备份耗时；日常记录 Job wall_ms |
| `frontend/src/components/TaskResultView.tsx` | LatencySection：默认汇总 chips + ▸ 展开详细时延（瀑布行/子行） |
| `frontend/src/styles/index.css` | lat-sec/lat-rows/lat-toggle/strong 样式 |

镜像：`inference-scheduler:v3.0.3`（hospital/clinic/dc 仍为 v3.0）。

---
### v3.0.1 / v3.0.2 - 修复（正式版 v3.0 后补丁）

| 特性 | 说明 |
|---|---|
| v3.0.1 | 修复 scheduler `get_task_result` 缺少 `redis_get` 导入导致的 500 |
| v3.0.2 | 诊断流水线 medical-server URL 归一化（自动补 `/infer`），clinic 发起转诊诊断全部通过 |

镜像：`inference-scheduler:v3.0.2`（hospital/clinic/dc 仍为 v3.0）。

---

### v3.0 - 正式版：云/边/端架构 + 四类任务（当前版本，重要更新 *）

| 特性 | 说明 |
|---|---|
| 三层架构 | Data Center(node3,云)：scheduler/medical-server/dc-services(患者库+协同计算)/redis；hospital(边)：诊断 worker；clinic(端)：任务发起与 peer |
| 移除 AlexNet | part1/part2 部署、流水线、镜像、前端入口全部下线/归档 `k8s/legacy/`；医院镜像不再含 part1 模型 |
| 调度继承 | scheduler 迁移至 node3（云数据中心） |
| 诊断 diagnosis | medical DoubleTower：hospital worker 前端 → DC medical-server；clinic 发起自动/指定转诊 hospital |
| 计算 compute | 模拟多监护仪器产数，发起 Pod + 另一边缘 Pod + DC 多分区协同计算并聚合 |
| 通信 sync | 模拟患者信息库（dc-services `/db/*`），P2P 多 peer 并行同步 + 云端备份 |
| 日常 routine | 数据中心按节点空闲度创建一次性 Kubernetes Job 执行，读日志后删除 |
| 发起/执行详情 | 每任务含 initiator + stages[] + result_detail（bpCR/转诊、分区表、P2P 分块/吞吐、Job 清单） |
| 综合测试 | 前端综合测试页 + `test/run_v3_comprehensive.py` 混合执行四类任务并展示详情 |
| 文档 | 新增 `v3.0-重构报告.md`；README 改版 |

| 文件 | 变更内容 |
|---|---|
| `dc/*`、`k8s/dc-services.yaml` | 新增数据中心服务（患者库+计算） |
| `common/v3_common.py` | 新增确定性产数/分块共享逻辑 |
| `hospital/app.py`、`clinic/app.py` | v3：移除 part1/内存任务类型，新增 /v3/compute、/v3/sync peer；clinic 含 routine 入口 |
| `scheduler/*` | 移除 AlexNet；新增 diagnosis/compute/sync/routine 流水线；node_usage/kubeops；/schedule/* 四端点 |
| `frontend/*` | 四类提交/详情/综合测试页；移除 AlexNet 与样本集 |
| `k8s/part2-*` | 归档至 legacy |
| `model_profile.yaml` | 四类任务资源档 |

**部署**：镜像 v3.0（scheduler/hospital/clinic/dc），`bash deploy.sh` 顺序执行并清理 part2。

---

### v2.0.6 - 补丁：集群编辑器（点击切换详情 + clinic 删除）

| 特性 | 说明 |
|---|---|
| 点击切换 | 修复 Pod 拖拽与点击事件竞态：点击任一 Pod（hospital/clinic）立即切换右侧详情面板 |
| 删除 clinic | clinic 详情末尾增加「删除该 Clinic Pod」：默认 clinic-1/2 也可删除（应用后删除部署与实例；重置一键恢复）；hospital 为核心实体不可删除，仅可拖动/改参数 |
| 后端规则 | validate：医院实体强制保留；clinic 允许移除（warning）；apply 删除所有不在期望模型中的 clinic |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/ClusterMap.tsx` | 点击即选中（pointerdown 也选中），去除拖拽竞态 |
| `frontend/src/components/PodPanel.tsx` | clinic 恒显示删除按钮 + 说明文案 |
| `frontend/src/pages/ClusterPage.tsx` | 允许删除默认 clinic、删除后自动切换选中实体 |
| `scheduler/cluster_config.py` | 默认医院不可移除；clinic 移除仅警告 |
| `scheduler/cluster_api.py` | 删除逻辑覆盖默认 clinic |
| `k8s/scheduler-deployment.yaml`、`build.sh` | scheduler v2.0.6 |

---

### v2.0.5 - 补丁：性能根治（任务 input 瘦身）+ AlexNet 轻量样本集 + part2 置信度归一化

| 特性 | 说明 |
|---|---|
| 总览/接口不再卡死 | 根因：任务记录把数 MB~10MB 的医疗输入整体存入 Redis，`/tasks`、`/tasks/stats` 每次全量取出解析（实测 17s+/次），并发后线程池耗尽→探针超时→scheduler 崩溃循环 |
| 输入瘦身 | 任务进入终态（finished/failed/completed）时自动移除 `input` 字段；存量历史任务已一次性清理（24 条清 21 条）；`/tasks/stats` 由 ~17s 降至 ~0.6s |
| 轻量总览 | 新增 `GET /cluster/summary`（节点负载+计数，无逐 Deployment 查询），总览页改用它；前端轮询间隔放宽（8s/15s/5s）并缩短 axios 超时 |
| AlexNet 轻量样本集 | 前端内置 6 张真实照片样本（public/samples，合计 ~110KB，无外网依赖）；提交默认“内置样本集”随机抽取，可换一张；保留上传/随机噪声（噪声仅用于链路测试并标注） |
| part2 softmax | part2 返回 raw logit 被当概率显示（485.7%）。修复：`score=softmax`，同时返回完整 `logits`；deployment 滚动策略改为 `maxUnavailable=1`（强制反亲和场景） |

| 文件 | 变更内容 |
|---|---|
| `scheduler/task_manager.py` | 终态任务移除 input |
| `scheduler/cluster_api.py` | `/cluster/status` 单次 pod list；新增 `/cluster/summary` |
| `frontend/public/samples/*.jpg` | 内置 6 张轻量样本 |
| `frontend/src/pages/SubmitPage.tsx` | AlexNet 内置样本集模式；轮询/内联详情优化 |
| `frontend/src/api.ts`、`OverviewPage.tsx`、`types.ts` | clusterSummary + 轮询频率与超时 |
| `part2/app.py` | softmax 置信度 + logits 输出 |
| `k8s/part2-deployment.yaml` | 镜像 v2.0.1 + 滚动策略 maxUnavailable=1 |
| `k8s/scheduler-deployment.yaml`、`build.sh` | scheduler v2.0.5 |

---

### v2.0.2 - 补丁：患者数据集/前端任务修复/新增 clinic 镜像兜底

| 特性 | 说明 |
|---|---|
| 患者可选 | scheduler 镜像内置 `test/test_dataset.json`，`/test/patients` 与 `/schedule/preprocessed` 在集群可用（此前 503） |
| AlexNet 修复 | 前端提交改为后端期望的 `input.image` 结构（此前顶层 image 导致 “No image data provided”） |
| 新增 clinic | 服务端对空镜像自动兜底默认 clinic 镜像（此前 k8s 422 Unprocessable）；前端新增 clinic 也自带默认镜像 |
| reset 不再降级 | 默认模型 clinic/scheduler 镜像指向修复版 tag（此前 reset 会把 clinic 打回有解析 bug 的 v2.0） |
| 错误可读性 | k8s ApiException 返回真实校验原因（不再截断 200 字符） |

| 文件 | 变更内容 |
|---|---|
| `scheduler/Dockerfile` | COPY test/test_dataset.json → /app/test/ |
| `scheduler/cluster_config.py` | 默认镜像 clinic:v2.0.1、scheduler:v2.0.2；canonicalize 对空镜像按类型兜底 |
| `scheduler/cluster_api.py` | `_k8s_err()` 输出 k8s 校验原文 |
| `frontend/src/api.ts` | submitAlexNet 改为 `input: {image}` |
| `frontend/src/utils.ts`、`ClusterPage.tsx` | 默认 clinic 镜像常量、新增 clinic 自带镜像 |
| `build.sh` | 按镜像分别 tag 构建推送 |

---

### v2.0.1 - 补丁：clinic 用量解析与 /health 探测修正

| 特性 | 说明 |
|---|---|
| clinic 修复 | 指定目标 Pod 时正确解析 k8s 内存量纲（`478Mi`/`1Gi` 等），usage_bytes 与 usage_percent 恢复正常 |
| scheduler 修复 | `/health` 对 part2 改探测根路径 `/`（part2 未提供 `/health`） |
| 镜像 | `inference-scheduler:v2.0.1`、`clinic:v2.0.1`（hospital:v2.0 内容未变） |

| 文件 | 变更内容 |
|---|---|
| `clinic/app.py` | 新增 `_parse_mem()` 用量/上限统一解析 |
| `scheduler/api.py` | part2 健康探测路径修正 |
| `build.sh` | 默认 TAG 改 v2.0.1 |

---

### v2.0 - 医院/诊所 Pod 化架构（当前版本，重要更新 *）

**变更目标**：把「node 即医院、worker/part1 独立部署」改为**实体 = Pod**，node1/node2 下各一个
hospital Pod（合并原 medical-worker + alexnet-part1 功能），并新增两个 clinic Pod 提供
「查询指定 Pod 内存占用率」任务（默认自身 cgroup，可指定目标经 metrics-server）。
同时全新设计 React 前端并新增集群编排（地图拖拽 + 应用/重置）。

| 特性 | 说明 |
|---|---|
| hospital Pod | `hospital-a @node1`、`hospital-b @node2`；单 Pod 暴露 `/medical/infer` 与 `/alexnet/infer`；`alexnet` 构造改为 `weights=None`+本地 `part1.pt`，消除运行期联网下载 |
| clinic Pod | `clinic-1 @node1`、`clinic-2 @node2`；`POST /query/mem {target_pod?}`，默认自身 cgroup，指定目标走 metrics-server |
| 实体路由 | 删除 `part1-service`/`medical-worker-service`；scheduler 按 hospital-a/b、clinic-1/2 服务路由三类任务（新增 `clinic` model） |
| /cluster/* API | status / default / validate / apply / reset / restart；scheduler SA 经 RBAC 真实下发（只改 hospital/clinic 部署） |
| 集群编辑器 | React + SVG 集群地图：大圆=节点(负载环)，小圆=Pod，支持拖动、新增 clinic、副本/亲和/资源/镜像/标签编辑 |
| 三类任务提交 | 医疗 bpCR、AlexNet 分类、clinic 内存监控；统一进入 /tasks 记录与统计 |
| 前端 | 全新 React18+Vite+TS（HashRouter），构建产物打进 scheduler 镜像于 `/app` 托管 |

**问题分析 → 解决方案**

- *问题*：part1 镜像运行期 `torchvision.alexnet(weights=IMAGENET1K_V1)` 联网下载，节点无外网时启动失败。
  → *方案*：hospital `alexnet_part1` 用 `weights=None` 构造后直接 `load_state_dict(part1.pt)`。
- *问题*：编辑器需要真实改集群，scheduler 默认 SA 无权限。
  → *方案*：新增 `k8s/scheduler-cluster-rbac.yaml`（deployments/services/pods/nodes 最小读写）与 `k8s/clinic-rbac.yaml`（clinic 读 pods/metrics）。
- *问题*：医院内存查询"指定目标"依赖 metrics-server。
  → *方案*：自身查询读 cgroup 恒可用；指定目标不可用返回结构化错误，前端明示。

| 文件 | 变更内容 |
|---|---|
| `hospital/app.py`、`hospital/Dockerfile` | 新增：worker+part1 合并服务 |
| `clinic/app.py`、`clinic/Dockerfile` | 新增：内存监控服务 |
| `scheduler/service_registry.py` | 新增：hospital/clinic 实体服务注册与路由 URL |
| `scheduler/cluster_config.py`、`cluster_api.py` | 新增：默认集群模型 + /cluster/* 编排 API |
| `scheduler/scheduler.py`、`policy.py`、`task_manager.py`、`metrics.py` | 实体路由 + clinic 流水线 + clinic 指标 |
| `scheduler/api.py` | `/schedule/clinic`、`/health` 探测实体、前端 dist 托管 |
| `frontend/*` | 全新 React 前端（4 页面 + 集群地图编辑器） |
| `k8s/hospital-*`、`k8s/clinic-*`、`k8s/*-rbac.yaml` | 新增部署/服务/RBAC |
| `k8s/legacy/` | 归档 part1 / medical-worker 清单 |
| `k8s/service-monitors.yaml` | part1/worker → hospital/clinic |
| `model_profile.yaml` | 增加 clinic.mem |
| `README.md` / `版本记录.md` | 架构与版本文档重写 |
| `deploy.sh`/`build.sh`/`start_all.sh`/`stop_all.sh` | v2.0 流程（镜像含前端） |

**部署**

| 项目 | 值 |
|---|---|
| scheduler 镜像 | `10.29.182.66:5000/k8s-repo/inference-scheduler:v2.0`（含前端 dist） |
| hospital 镜像 | `…/k8s-repo/hospital:v2.0`（含 medical 权重 + part1.pt） |
| clinic 镜像 | `…/k8s-repo/clinic:v2.0` |
| 实体 | hospital-a/b、clinic-1/2（scheduler 前端 `/app` 访问；NodePort 30080） |

---

### v1.5 - 调度缺陷修复（历史）

| 特性 | 说明 |
|---|---|
| 双队列机制 | Redis 优先级队列 + 进程内 PriorityTaskQueue 回退 |
| 连接池/重试 | requests.Session + Redis 连接池，指数退避重试 |
| 指标修复 | Prometheus 计数/直方图按 model 上报 |
| 分析文档 | TASK_DISPATCH_ANALYSIS.md（节点选择 ≠ 请求路由的根因分析） |

### v1.4 - 调度与监控解耦（历史）

| 特性 | 说明 |
|---|---|
| monitoring 服务 | Prometheus 查询代理（/nodes、/latency/*） |
| prediction 服务 | 延迟预测 / 分区点优化 / 自动重训 |
| 指标端口分离 | 业务 8000 / Prometheus 9000 |

### v1.3 - 任务生命周期与前端托管（历史）

| 特性 | 说明 |
|---|---|
| /tasks 全量 API | 列表/统计/删除/清空 |
| 前端静态托管 | scheduler /app 提供 Web Dashboard |
| worker/server 状态机 | create→queue→dispatch→running→finished/failed |

### v1.2 - 推理服务稳定性修复（历史）

| 特性 | 说明 |
|---|---|
| 镜像瘦身 | 多阶段 + CPU-only PyTorch |
| 探针与资源 | 就绪/存活探针、requests/limits 校准 |
| 模型加载守卫 | 权重缺失时降级 503 而非崩溃 |

### v1.1 - 多阶段构建修复（历史）

| 特性 | 说明 |
|---|---|
| 基础镜像 | python:3.11-slim 多阶段构建 |
| 依赖收敛 | uvicorn 以 `python -m uvicorn` 启动 |

### v1.0 - 多模型边云协同平台搭建（历史，*）

| 特性 | 说明 |
|---|---|
| 双模型流水线 | AlexNet 拆分 part1/part2 + 鹏城医疗 DoubleTower 拆分 worker/server |
| 三级调度 | 优先级队列 → 资源评分 → 模型约束 |
| 节点拓扑 | node1/node2=edge(医院)、node3=cloud、控制面 scheduler |
| 监控栈 | Prometheus/Grafana/ServiceMonitor |
