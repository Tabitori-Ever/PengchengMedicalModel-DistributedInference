# 版本记录

> 本文档记录 PengchengMedicalModel-DistributedInference 平台的版本演进。
> 版本号 = 镜像 tag / 代码里程碑；`*` 表示重要更新。
> 历史格式沿用 AlexNet-K8s 的 `### vX[.Y] - 标题` + 特性表风格。

## 版本演进图

```
v1.0 多模型边云协同平台搭建*
  ↓
v1.1 镜像瘦身与多阶段构建
  ↓
v1.2 推理服务稳定性修复（连接池/重试/指标）
  ↓
v1.3 任务生命周期完善 + 前端静态托管
  ↓
v1.4 调度与监控解耦（prediction/monitoring 服务）
  ↓
v1.5 调度缺陷修复（实体路由分析/双队列机制）
  ↓
v2.0 医院/诊所 Pod 化架构 + 新前端 + 集群编辑器*
  ↓
v2.0.1 补丁：clinic 用量解析、/health part2 探测
  ↓
v2.0.2 补丁：内置患者数据集、前端 AlexNet 请求结构、新增 clinic 镜像兜底
  ↓
v2.0.3 补丁：节点负载 metrics-server 实时化、提交结果内联详情
  ↓
v2.0.4/v2.0.5 补丁：任务 input 瘦身(性能根治) + /cluster/summary +
               AlexNet 内置轻量样本集 + part2 置信度归一化
  ↓
v2.0.6 补丁：集群编辑器点击切换详情、clinic Pod 可删除(重置恢复)
  ↓
v3.0 正式版：云/边/端架构 + 四类任务(诊断/计算/通信/日常)*
      移除 AlexNet(part1/part2)，调度功能由数据中心继承   ← 当前版本
```

---

### v3.0.1 / v3.0.2 - 修复（正式版 v3.0 后补丁）

| 特性 | 说明 |
|---|---|
| v3.0.1 | 修复 scheduler `get_task_result` 缺少 `redis_get` 导入导致的 500 |
| v3.0.2 | 诊断流水线 medical-server URL 归一化（自动补 `/infer`），clinic 发起转诊诊断全部通过 |

镜像：`inference-scheduler:v3.0.2`（hospital/clinic/dc 仍为 v3.0）。

---

### v3.0 - 正式版：云/边/端架构 + 四类任务（当前版本，重要更新 *）

| 特性 | 说明 |
|---|---|
| 三层架构 | Data Center(node3,云)：scheduler/medical-server/dc-services(患者库+协同计算)/redis；hospital(边)：诊断 worker；clinic(端)：任务发起与 peer |
| 移除 AlexNet | part1/part2 部署、流水线、镜像、前端入口全部下线/归档 `k8s/legacy/`；医院镜像不再含 part1 模型 |
| 调度继承 | scheduler 迁移至 node3（云数据中心） |
| 诊断 diagnosis | medical DoubleTower：hospital worker 前端 → DC medical-server；clinic 发起自动/指定转诊 hospital |
| 计算 compute | 模拟多监护仪器产数，发起 Pod + 另一边缘 Pod + DC 多分区协同计算并聚合 |
| 通信 sync | 模拟患者信息库（dc-services `/db/*`），P2P 多 peer 并行同步 + 云端备份 |
| 日常 routine | 数据中心按节点空闲度创建一次性 Kubernetes Job 执行，读日志后删除 |
| 发起/执行详情 | 每任务含 initiator + stages[] + result_detail（bpCR/转诊、分区表、P2P 分块/吞吐、Job 清单） |
| 综合测试 | 前端综合测试页 + `test/run_v3_comprehensive.py` 混合执行四类任务并展示详情 |
| 文档 | 新增 `v3.0-重构报告.md`；README 改版 |

| 文件 | 变更内容 |
|---|---|
| `dc/*`、`k8s/dc-services.yaml` | 新增数据中心服务（患者库+计算） |
| `common/v3_common.py` | 新增确定性产数/分块共享逻辑 |
| `hospital/app.py`、`clinic/app.py` | v3：移除 part1/内存任务类型，新增 /v3/compute、/v3/sync peer；clinic 含 routine 入口 |
| `scheduler/*` | 移除 AlexNet；新增 diagnosis/compute/sync/routine 流水线；node_usage/kubeops；/schedule/* 四端点 |
| `frontend/*` | 四类提交/详情/综合测试页；移除 AlexNet 与样本集 |
| `k8s/part2-*` | 归档至 legacy |
| `model_profile.yaml` | 四类任务资源档 |

**部署**：镜像 v3.0（scheduler/hospital/clinic/dc），`bash deploy.sh` 顺序执行并清理 part2。

---

### v2.0.6 - 补丁：集群编辑器（点击切换详情 + clinic 删除）

| 特性 | 说明 |
|---|---|
| 点击切换 | 修复 Pod 拖拽与点击事件竞态：点击任一 Pod（hospital/clinic）立即切换右侧详情面板 |
| 删除 clinic | clinic 详情末尾增加「删除该 Clinic Pod」：默认 clinic-1/2 也可删除（应用后删除部署与实例；重置一键恢复）；hospital 为核心实体不可删除，仅可拖动/改参数 |
| 后端规则 | validate：医院实体强制保留；clinic 允许移除（warning）；apply 删除所有不在期望模型中的 clinic |

| 文件 | 变更内容 |
|---|---|
| `frontend/src/components/ClusterMap.tsx` | 点击即选中（pointerdown 也选中），去除拖拽竞态 |
| `frontend/src/components/PodPanel.tsx` | clinic 恒显示删除按钮 + 说明文案 |
| `frontend/src/pages/ClusterPage.tsx` | 允许删除默认 clinic、删除后自动切换选中实体 |
| `scheduler/cluster_config.py` | 默认医院不可移除；clinic 移除仅警告 |
| `scheduler/cluster_api.py` | 删除逻辑覆盖默认 clinic |
| `k8s/scheduler-deployment.yaml`、`build.sh` | scheduler v2.0.6 |

---

### v2.0.5 - 补丁：性能根治（任务 input 瘦身）+ AlexNet 轻量样本集 + part2 置信度归一化

| 特性 | 说明 |
|---|---|
| 总览/接口不再卡死 | 根因：任务记录把数 MB~10MB 的医疗输入整体存入 Redis，`/tasks`、`/tasks/stats` 每次全量取出解析（实测 17s+/次），并发后线程池耗尽→探针超时→scheduler 崩溃循环 |
| 输入瘦身 | 任务进入终态（finished/failed/completed）时自动移除 `input` 字段；存量历史任务已一次性清理（24 条清 21 条）；`/tasks/stats` 由 ~17s 降至 ~0.6s |
| 轻量总览 | 新增 `GET /cluster/summary`（节点负载+计数，无逐 Deployment 查询），总览页改用它；前端轮询间隔放宽（8s/15s/5s）并缩短 axios 超时 |
| AlexNet 轻量样本集 | 前端内置 6 张真实照片样本（public/samples，合计 ~110KB，无外网依赖）；提交默认“内置样本集”随机抽取，可换一张；保留上传/随机噪声（噪声仅用于链路测试并标注） |
| part2 softmax | part2 返回 raw logit 被当概率显示（485.7%）。修复：`score=softmax`，同时返回完整 `logits`；deployment 滚动策略改为 `maxUnavailable=1`（强制反亲和场景） |

| 文件 | 变更内容 |
|---|---|
| `scheduler/task_manager.py` | 终态任务移除 input |
| `scheduler/cluster_api.py` | `/cluster/status` 单次 pod list；新增 `/cluster/summary` |
| `frontend/public/samples/*.jpg` | 内置 6 张轻量样本 |
| `frontend/src/pages/SubmitPage.tsx` | AlexNet 内置样本集模式；轮询/内联详情优化 |
| `frontend/src/api.ts`、`OverviewPage.tsx`、`types.ts` | clusterSummary + 轮询频率与超时 |
| `part2/app.py` | softmax 置信度 + logits 输出 |
| `k8s/part2-deployment.yaml` | 镜像 v2.0.1 + 滚动策略 maxUnavailable=1 |
| `k8s/scheduler-deployment.yaml`、`build.sh` | scheduler v2.0.5 |

---

### v2.0.2 - 补丁：患者数据集/前端任务修复/新增 clinic 镜像兜底

| 特性 | 说明 |
|---|---|
| 患者可选 | scheduler 镜像内置 `test/test_dataset.json`，`/test/patients` 与 `/schedule/preprocessed` 在集群可用（此前 503） |
| AlexNet 修复 | 前端提交改为后端期望的 `input.image` 结构（此前顶层 image 导致 “No image data provided”） |
| 新增 clinic | 服务端对空镜像自动兜底默认 clinic 镜像（此前 k8s 422 Unprocessable）；前端新增 clinic 也自带默认镜像 |
| reset 不再降级 | 默认模型 clinic/scheduler 镜像指向修复版 tag（此前 reset 会把 clinic 打回有解析 bug 的 v2.0） |
| 错误可读性 | k8s ApiException 返回真实校验原因（不再截断 200 字符） |

| 文件 | 变更内容 |
|---|---|
| `scheduler/Dockerfile` | COPY test/test_dataset.json → /app/test/ |
| `scheduler/cluster_config.py` | 默认镜像 clinic:v2.0.1、scheduler:v2.0.2；canonicalize 对空镜像按类型兜底 |
| `scheduler/cluster_api.py` | `_k8s_err()` 输出 k8s 校验原文 |
| `frontend/src/api.ts` | submitAlexNet 改为 `input: {image}` |
| `frontend/src/utils.ts`、`ClusterPage.tsx` | 默认 clinic 镜像常量、新增 clinic 自带镜像 |
| `build.sh` | 按镜像分别 tag 构建推送 |

---

### v2.0.1 - 补丁：clinic 用量解析与 /health 探测修正

| 特性 | 说明 |
|---|---|
| clinic 修复 | 指定目标 Pod 时正确解析 k8s 内存量纲（`478Mi`/`1Gi` 等），usage_bytes 与 usage_percent 恢复正常 |
| scheduler 修复 | `/health` 对 part2 改探测根路径 `/`（part2 未提供 `/health`） |
| 镜像 | `inference-scheduler:v2.0.1`、`clinic:v2.0.1`（hospital:v2.0 内容未变） |

| 文件 | 变更内容 |
|---|---|
| `clinic/app.py` | 新增 `_parse_mem()` 用量/上限统一解析 |
| `scheduler/api.py` | part2 健康探测路径修正 |
| `build.sh` | 默认 TAG 改 v2.0.1 |

---

### v2.0 - 医院/诊所 Pod 化架构（当前版本，重要更新 *）

**变更目标**：把「node 即医院、worker/part1 独立部署」改为**实体 = Pod**，node1/node2 下各一个
hospital Pod（合并原 medical-worker + alexnet-part1 功能），并新增两个 clinic Pod 提供
「查询指定 Pod 内存占用率」任务（默认自身 cgroup，可指定目标经 metrics-server）。
同时全新设计 React 前端并新增集群编排（地图拖拽 + 应用/重置）。

| 特性 | 说明 |
|---|---|
| hospital Pod | `hospital-a @node1`、`hospital-b @node2`；单 Pod 暴露 `/medical/infer` 与 `/alexnet/infer`；`alexnet` 构造改为 `weights=None`+本地 `part1.pt`，消除运行期联网下载 |
| clinic Pod | `clinic-1 @node1`、`clinic-2 @node2`；`POST /query/mem {target_pod?}`，默认自身 cgroup，指定目标走 metrics-server |
| 实体路由 | 删除 `part1-service`/`medical-worker-service`；scheduler 按 hospital-a/b、clinic-1/2 服务路由三类任务（新增 `clinic` model） |
| /cluster/* API | status / default / validate / apply / reset / restart；scheduler SA 经 RBAC 真实下发（只改 hospital/clinic 部署） |
| 集群编辑器 | React + SVG 集群地图：大圆=节点(负载环)，小圆=Pod，支持拖动、新增 clinic、副本/亲和/资源/镜像/标签编辑 |
| 三类任务提交 | 医疗 bpCR、AlexNet 分类、clinic 内存监控；统一进入 /tasks 记录与统计 |
| 前端 | 全新 React18+Vite+TS（HashRouter），构建产物打进 scheduler 镜像于 `/app` 托管 |

**问题分析 → 解决方案**

- *问题*：part1 镜像运行期 `torchvision.alexnet(weights=IMAGENET1K_V1)` 联网下载，节点无外网时启动失败。
  → *方案*：hospital `alexnet_part1` 用 `weights=None` 构造后直接 `load_state_dict(part1.pt)`。
- *问题*：编辑器需要真实改集群，scheduler 默认 SA 无权限。
  → *方案*：新增 `k8s/scheduler-cluster-rbac.yaml`（deployments/services/pods/nodes 最小读写）与 `k8s/clinic-rbac.yaml`（clinic 读 pods/metrics）。
- *问题*：医院内存查询"指定目标"依赖 metrics-server。
  → *方案*：自身查询读 cgroup 恒可用；指定目标不可用返回结构化错误，前端明示。

| 文件 | 变更内容 |
|---|---|
| `hospital/app.py`、`hospital/Dockerfile` | 新增：worker+part1 合并服务 |
| `clinic/app.py`、`clinic/Dockerfile` | 新增：内存监控服务 |
| `scheduler/service_registry.py` | 新增：hospital/clinic 实体服务注册与路由 URL |
| `scheduler/cluster_config.py`、`cluster_api.py` | 新增：默认集群模型 + /cluster/* 编排 API |
| `scheduler/scheduler.py`、`policy.py`、`task_manager.py`、`metrics.py` | 实体路由 + clinic 流水线 + clinic 指标 |
| `scheduler/api.py` | `/schedule/clinic`、`/health` 探测实体、前端 dist 托管 |
| `frontend/*` | 全新 React 前端（4 页面 + 集群地图编辑器） |
| `k8s/hospital-*`、`k8s/clinic-*`、`k8s/*-rbac.yaml` | 新增部署/服务/RBAC |
| `k8s/legacy/` | 归档 part1 / medical-worker 清单 |
| `k8s/service-monitors.yaml` | part1/worker → hospital/clinic |
| `model_profile.yaml` | 增加 clinic.mem |
| `README.md` / `版本记录.md` | 架构与版本文档重写 |
| `deploy.sh`/`build.sh`/`start_all.sh`/`stop_all.sh` | v2.0 流程（镜像含前端） |

**部署**

| 项目 | 值 |
|---|---|
| scheduler 镜像 | `10.29.182.66:5000/k8s-repo/inference-scheduler:v2.0`（含前端 dist） |
| hospital 镜像 | `…/k8s-repo/hospital:v2.0`（含 medical 权重 + part1.pt） |
| clinic 镜像 | `…/k8s-repo/clinic:v2.0` |
| 实体 | hospital-a/b、clinic-1/2（scheduler 前端 `/app` 访问；NodePort 30080） |

---

### v1.5 - 调度缺陷修复（历史）

| 特性 | 说明 |
|---|---|
| 双队列机制 | Redis 优先级队列 + 进程内 PriorityTaskQueue 回退 |
| 连接池/重试 | requests.Session + Redis 连接池，指数退避重试 |
| 指标修复 | Prometheus 计数/直方图按 model 上报 |
| 分析文档 | TASK_DISPATCH_ANALYSIS.md（节点选择 ≠ 请求路由的根因分析） |

### v1.4 - 调度与监控解耦（历史）

| 特性 | 说明 |
|---|---|
| monitoring 服务 | Prometheus 查询代理（/nodes、/latency/*） |
| prediction 服务 | 延迟预测 / 分区点优化 / 自动重训 |
| 指标端口分离 | 业务 8000 / Prometheus 9000 |

### v1.3 - 任务生命周期与前端托管（历史）

| 特性 | 说明 |
|---|---|
| /tasks 全量 API | 列表/统计/删除/清空 |
| 前端静态托管 | scheduler /app 提供 Web Dashboard |
| worker/server 状态机 | create→queue→dispatch→running→finished/failed |

### v1.2 - 推理服务稳定性修复（历史）

| 特性 | 说明 |
|---|---|
| 镜像瘦身 | 多阶段 + CPU-only PyTorch |
| 探针与资源 | 就绪/存活探针、requests/limits 校准 |
| 模型加载守卫 | 权重缺失时降级 503 而非崩溃 |

### v1.1 - 多阶段构建修复（历史）

| 特性 | 说明 |
|---|---|
| 基础镜像 | python:3.11-slim 多阶段构建 |
| 依赖收敛 | uvicorn 以 `python -m uvicorn` 启动 |

### v1.0 - 多模型边云协同平台搭建（历史，*）

| 特性 | 说明 |
|---|---|
| 双模型流水线 | AlexNet 拆分 part1/part2 + 鹏城医疗 DoubleTower 拆分 worker/server |
| 三级调度 | 优先级队列 → 资源评分 → 模型约束 |
| 节点拓扑 | node1/node2=edge(医院)、node3=cloud、控制面 scheduler |
| 监控栈 | Prometheus/Grafana/ServiceMonitor |
