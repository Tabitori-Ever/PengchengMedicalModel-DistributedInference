# v3.0 重构报告（正式版）—— 云 / 边 / 端 架构与四类任务

> 版本：v3.0（正式版）　|　上一版：v2.0.x（医院/诊所 Pod 化）
> 本报告记录 v3.0 的集群架构、任务模型、执行详情、综合测试与迁移方式。

---

## 1. 背景与变更目标

v2.x 中平台以“医院(worker+part1) / 诊所(内存监控) + 云端(server/part2)”组织任务，其中
AlexNet(part1/part2) 图像分类与诊所内存监控作为独立任务类型。v3.0 决定：

1. **移除 AlexNet(part1/part2) 全部功能** —— 集群不再执行图像分类任务，相关部署/镜像/代码/监控全部下线或归档。
2. **明确三层架构**：医院 hospital = **边**（原始数据不出院），诊所 clinic = **端**（业务发起/轻量终端），数据中心 Data Center = **云**（高算力、患者库、调度）。
3. **调度功能由数据中心继承**：scheduler（原控制平面）迁至云节点 node3，与 medical-server、redis、患者库同处数据中心。
4. **任务收敛为四类**，均由 hospital / clinic 发起：
   - **诊断 diagnosis**：医疗模型推理（hospital worker 前段 → DC medical-server 后段）；clinic 发起时自动/指定转诊 hospital。
   - **计算 compute**：多监护仪器产数 + 多 Pod（边/端/云）协同计算。
   - **通信 sync**：患者信息库云更新/云同步，P2P 多 peer 并行传输 + 云端备份。
   - **日常 routine**：由数据中心调度器按节点空闲情况创建一次性 Job Pod 执行，完成后删除。
5. 每个任务结果展示**发起详情 + 执行详情**，可选择发起 Pod。
6. 提供**综合测试**：四类任务混合执行并展示详细结果。

---

## 2. 集群架构 v3.0（云/边/端）

```
                    ┌───────────────────────────────────────────────┐
                    │        Data Center 数据中心 (node3 · 云)        │
                    │  scheduler(调度继承) · medical-server · redis   │
                    │  dc-services: 患者库 patient-db + 协同计算/备份    │
                    └────────────▲───────────────▲──────────────────┘
                  worker/server  │   分区/同步     │
                    ┌────────────┴───────┐   ┌───┴──────────────┐
                    │ hospital 医院 (边)   │   │ clinic 诊所 (端)   │
                    │ hospital-a@node1    │   │ clinic-1@node1    │
                    │ hospital-b@node2    │   │ clinic-2@node2    │
                    │ 诊断 worker 前端     │   │ 发起四类任务         │
                    │ 参与计算/同步 peer   │   │ 参与计算/同步 peer   │
                    └────────────────────┘   └──────────────────┘
```

| 层 | 节点 | 实体 | 组件/职责 |
|---|---|---|---|
| 云 Data Center | node3 | scheduler、medical-server、dc-services、redis | 调度与资源分配、诊断 server、患者库+备份、协同计算聚合、日常 Job |
| 边 hospital | node1/node2 | hospital-a/b | 诊断 worker（数据不出院）、发起任务、协同计算分区、P2P peer |
| 端 clinic | node1/node2 | clinic-1/2 | 发起四类任务、本地患者副本、P2P peer、日常 Job 常用镜像宿主 |
| 运维 | desktop | monitoring / prediction(可选) | 监控展示，不承载业务 |

- scheduler 由 desktop 迁移至 node3（云），`/cluster/*` 集群编辑不变（hospital/clinic 可编辑；dc-services 等只读）。
- part1/part2（AlexNet）Deployment/Service 下线并归档 `k8s/legacy/`；医院镜像不再包含 part1 模型。

---

## 3. 四类任务定义与流程

公共请求：`POST /schedule/{type}`，公共字段 `source(hospital-a|hospital-b|clinic-1|clinic-2)`, `priority`, `deadline`。
结果统一包含：`initiator`（发起实体/角色/节点/优先级/时间）、`stages[]`（执行阶段：名称/执行者/节点/耗时/说明）、各类型 `result_detail`。

### 3.1 诊断 diagnosis —— 医疗模型推理（云边协同）
- 流程：`hospital /medical/infer`（DoubleTower 前端，边）→ `medical-server /infer`（DC，云）→ bpCR 概率。
- 发起：hospital 直接本医院执行；**clinic 发起 → 自动选择空闲 hospital 转诊**（或前端指定 `target_hospital`）。
- 结果：`forwarded_to`（clinic 场景显示转诊医院）、bpCR、worker/server 两段延迟、stages。

### 3.2 计算 compute —— 多监护仪器协同计算
- 语义：模拟多台监护仪器（ECG/SpO2/血压等，`instruments` 1..8）在发起端批量产数（`rows`），
  拆分为多个分区（`partition_count` 2..6），由 **发起 Pod + 数据中心 + 另一边缘 Pod** 多实体协同计算
  （CPU 统计循环 + 分片校验和），DC 聚合。
- 调度：按 metrics-server 空闲选择参与实体。
- 结果：`produced`（产数规模）、`partitions[]`（每分区 actor/node/rows/bytes/checksum/cpu_ms/ms）、
  `result_detail`（成功分区数、总字节、总 CPU 时间、校验和列表）。

**多台监护仪器如何被“模拟产数”并构成计算任务**

- 每台“仪器”抽象为一路时序数据通道，模拟一次计算任务即让 `instruments` 台仪器**同时刻产出一批读数**。
- 产数由共享模块 `common/v3_common.py::gen_readings(rows, instruments, seed)` 完成：输出一个
  `instruments × rows` 的浮点矩阵（每元素 ∈ [0,1]，用 `random.Random(seed)` 确定性生成）——
  同一 seed 在任何 Pod 上都得到完全相同的数据，因此产数环节不需要在节点间搬运原始数据。
- 任务参数（`POST /schedule/compute`）与物理含义的对应：

| 参数 | 默认 | 含义（模拟层） |
|---|---|---|
| `instruments` | 4 | 同时工作的监护仪台数（如 ECG、SpO2、IBP、呼吸等通道数） |
| `rows` | 256 | 每台仪器的一轮采样点数（数据行数） |
| `intensity` | 40 | 每分区 CPU 计算强度（滚动统计 + SHA-256 增量摘要的重复遍数） |
| `partition_count` | 3 | 将数据拆给几个实体协同计算的分区数（上限 6） |

- **产数→分区的编排**：调度器先选出协作实体集合（发起端本身 + `datacenter` + 另一家 hospital，
  共 `partition_count` 个），随后每个协作实体各自在本地对“自己的分区”执行：
  `compute_partition(rows, instruments, intensity, seed=任务seed+分区号, partition, count)`
  ——由于产数是确定性的，各执行端按分区号重新生成局部读数即可，无需中心分发大批量数组。
- **“计算任务”的构成**：① 发起端完成“产数声明”（`produced{instruments, rows, samples}`，
  即整批监护数据的规模元数据）；② 各执行端计算分区（每分区返回：处理字节 ≈
  `rows×instruments×4×intensity`、`cpu_ms`、可复现的 `checksum`）；③ 数据中心汇总并做完整性呈现
  （成功分区数、总字节、合计 CPU、各分区校验和列表；同一分区参数重跑 checksum 一致，可用于对账）。

### 3.3 通信 sync —— 患者库 P2P 云同步与备份
- 患者库（dc-services `/db/*`）：模拟患者信息记录（共 64 条 base + 边缘上传），记录按块(4KB)可寻址，
  支持 manifest/下载/上传/备份/校验。
- 医院/诊所 Pod 各自持有本地副本（按实体稳定随机子集，且各有待上云的本地更新 2-3 条）。
- 流程（发起端视角）：取云 manifest → 计算缺失分块 → **并行(concurrency)从多个 peer 拉取**
  （peer=另一 hospital/clinic 或 DC，模拟 P2P 多源下载，按带宽节流）→ 本地 accepted →
  上传本地更新 → 云端备份快照。
- 结果：`chunks[]`（块/来源 peer/大小/耗时）、拉取/失败数、总字节与耗时、并发与带宽、
  上传数、备份 id/哈希。

**患者信息记录如何构成“数据库”并参与同步任务**

- **记录模型（一张“模拟患者信息库”）**：云端主库由 `DB_VERSION`（默认 `v3-db-1`）与
  `DB_ITEM_COUNT`（默认 64）确定初始记录集——每条记录 id 为 `rec-{版本}-{序号}`（由
  `item_ids()` 确定性生成），记录内容（blob）为固定 4KB 的确定性字符串（`chunk_blob()`），
  并附 sha256 前 24 位作为记录哈希；`dc-services` 以内存主库形式提供：
  `GET /db/state`（记录全集/总数/备份数）、`GET /db/item/{id}`（按 id 取内容）、
  `POST /db/upload`（校验哈希后并入云端并记入 uploads 日志）、`POST /db/backup`（全量快照：
  记录总数 + 整体哈希）。
- **端/边上的副本与增量**：每个 hospital / clinic Pod 启动时按
  `hold_subset(all_ids, secret="{实体}:{节点}", ratio)` 稳定筛选出“本地已持有”记录
  （hospital 默认 0.7、clinic 默认 0.5），即本地数据库副本是一个确定性子集；同时每个实体
  本地预生成 2~3 条“待上云的本地更新”（`upd-{实体}-i`），代表该医院/诊所产生的患者信息增量。
- **同步任务 = 用上述副本拓扑做增量对齐 + 云备份**：调度器取云记录全集并与发起端本地
  `holds` 求差得到“缺失集合”→ 发起端按并发度从多个 peer（另一 hospital/clinic 或云备份）
  并行拉取缺失记录（peer 侧按 `chunk_blob` 确定性应答，调度器按带宽限速模拟传输耗时）→
  拉取成功的记录回写本地 `accepted` → 上传本端 pending 更新 → 云端 `backup` 快照。
- 因此“患者信息记录形成数据库”：主库 = DC 的 64+ 条记录；每端副本 = 确定性子集；
  云同步 = 子集向全量收敛、本地增量上云并产生可校验的备份。

### 3.4 日常 routine —— 调度器生成一次性 Job Pod
- 数据中心调度器读取各节点空闲度（metrics-server），为每个分区在目标节点创建 **Kubernetes Job**
  （镜像 clinic:v3.0，执行内置 `/app/run_routine.py` 日常计算并输出 JSON），
  轮询完成 → 读取 Job pod 日志解析 → 写入任务结果 → **删除 Job**。
- 发起：hospital/clinic 手动发起或综合测试批量触发；可选周期队列（env `ROUTINE_AUTO_MINUTES`，默认 0=关）。
- 结果：`jobs[]`（Job 名/节点/状态/输出 cpu_ms/checksum/deleted 标记）、成功数。

---

## 4. API 与前端

### 4.1 提交/查询
| 接口 | 方法 | 说明 |
|---|---|---|
| `/schedule/diagnosis` | POST | 诊断（patient_id 或原始 input；clinic→转诊 hospital） |
| `/schedule/compute` | POST | 协同计算（instruments/rows/intensity/partition_count） |
| `/schedule/sync` | POST | P2P 云同步（bandwidth_mbps/concurrency/chunk_kb） |
| `/schedule/routine` | POST | 日常（jobs/rows/intensity） |
| `/task/result/{id}`、`/tasks`、`/tasks/stats`、`/tasks/{id}` | GET/DELETE | 查询与记录 |
| `/test/patients` | GET | 测试患者（诊断患者选择） |
| `/cluster/*` | GET/POST/PUT | 集群编辑（沿用 v2，实体仍为 hospital/clinic） |

### 4.2 前端（React v3.0，/app）
- 总览：云/边/端描述 + 四类统计 + 最近任务。
- 提交：诊断/计算/通信/日常四个 Tab，均带**发起 Pod 选择器**；诊断显示 clinic→hospital 转诊。
- 记录/详情：四类详情卡片（诊断 bpCR+转诊、计算分区表、P2P 分块/吞吐/备份、日常 Job 清单）+
  发起详情（发起实体/时间/优先级）+ 阶段列表。
- **综合测试页**：可配比（默认 诊断2[1 条 clinic 转诊]、计算1、通信1、日常3）一键执行并展示每任务详情。

---

## 5. 数据与模拟机制

- 监护仪器产数与分块负载全部**确定性生成**（同一 seed 在任何 Pod 结果一致），便于校验；不依赖外网。
- 患者库记录为模拟元数据（复用 10 名测试患者标识 + 派生记录），无真实患者数据外泄。
- P2P 同步的“闲置带宽”用可配置节流模拟（bandwidth_mbps 越小耗时越长）。
- 日常 Job 使用轻量 clinic 镜像（内置 routine 入口），避免大镜像重复拉取。

---

## 6. 综合测试

- CLI：`python test/run_v3_comprehensive.py [--diagnosis 2 --compute 1 --sync 1 --routine 3]`
  输出每任务状态与结果摘要，完整 JSON 保存 `test/output/v3_comprehensive.json`。
- 前端：集群编排页 → 顶部“综合测试”按钮/独立页执行同一混合套件，实时展示执行详情。

---

## 7. 部署与版本

- 镜像（全部 v3.0）：`inference-scheduler`、`hospital`、`clinic`、`dc`；medical-server 沿用 v1.0。
- 迁移步骤（自 v2.0.x）：构建推送 v3.0 镜像 → `bash deploy.sh`
  （RBAC→dc-services→hospital→clinic→medical-server→scheduler 迁 node3→清理 part2）→ 健康检查 → 综合测试。
- Git：commit 分组（移除 AlexNet → DC/四类任务 → 前端 → 测试/文档），tag `v3.0`（正式版）。
- 版本记录：`版本记录.md` v3.0 区块。
