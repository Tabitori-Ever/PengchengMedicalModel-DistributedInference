# Prediction service: only needs HTTP + sklearn + Redis
# No PyTorch needed — it only does latency prediction, not inference
fastapi>=0.100.0
uvicorn>=0.22.0
requests>=2.28.0
prometheus_client>=0.17.0
redis>=4.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
