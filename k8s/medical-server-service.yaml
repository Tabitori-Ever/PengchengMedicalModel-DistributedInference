apiVersion: v1
kind: Service

metadata:
  name: medical-server-service
  labels:
    app: medical-server
    model: medical
    stage: server

spec:
  selector:
    app: medical-server

  ports:
  - name: http
    protocol: TCP
    port: 9001
    targetPort: 9001

  type: ClusterIP
