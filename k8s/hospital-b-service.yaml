apiVersion: v1
kind: Service

metadata:
  name: hospital-b-service
  labels:
    app: hospital
    hospital: hospital-b

spec:
  selector:
    app: hospital
    hospital: hospital-b

  ports:
  - name: http
    protocol: TCP
    port: 8006
    targetPort: 8006

  type: ClusterIP
