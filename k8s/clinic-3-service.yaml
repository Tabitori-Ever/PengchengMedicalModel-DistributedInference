apiVersion: v1
kind: Service

metadata:
  name: clinic-3-service
  labels:
    app: clinic
    clinic: clinic-3

spec:
  selector:
    app: clinic
    clinic: clinic-3

  ports:
  - name: http
    protocol: TCP
    port: 8007
    targetPort: 8007

  type: ClusterIP
