apiVersion: v1
kind: Service

metadata:
  name: clinic-2-service
  labels:
    app: clinic
    clinic: clinic-2

spec:
  selector:
    app: clinic
    clinic: clinic-2

  ports:
  - name: http
    protocol: TCP
    port: 8007
    targetPort: 8007

  type: ClusterIP
