apiVersion: v1
kind: Service

metadata:
  name: clinic-1-service
  labels:
    app: clinic
    clinic: clinic-1

spec:
  selector:
    app: clinic
    clinic: clinic-1

  ports:
  - name: http
    protocol: TCP
    port: 8007
    targetPort: 8007

  type: ClusterIP
