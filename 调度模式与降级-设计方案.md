# 执行模式选择 · 调度降级 · 测试与性能对比站点 —— 设计方案（v3.2）

> 本文是 v3.2 改造的实现契约：**执行模式语义、统一结果口径、边缘 Pod `/local/*` 接口、
> scheduler `mode` 支持、新站点 `/api/*` 接口**。实现方（后端 / 前端 / 站点）以此为准。

---

## 1. 背景：现状与关键事实

| 事实 | 证据 | 结论 |
|---|---|---|
| 任务发起 = 浏览器 → scheduler(NodePort 30080) → 编排边缘/云多角色 | `frontend/src/api.ts`、`scheduler/api.py` | 边缘 Pod 目前**没有**自己完成整任务的入口 |
| 前端整站由 scheduler Pod 托管 | `scheduler/api.py` 挂载 `/app` | scheduler 掉线 → 旧 UI 一起消失，**降级测试必须在独立站点做** |
| clinic 镜像无 PyTorch / 无模型 | `clinic/Dockerfile`、`clinic/requirements.txt` | 诊所**不具备**完整诊断能力 |
| hospital 镜像已含完整 DoubleTower 代码 + 完整权重 | `hospital/Dockerfile`（拷 `models_my.py` + 全量 `best_model_fold1.pth`）、`hospital/app.py::load_medical_model` 用 `strict=True` 加载全模型，只取前端子模块 | 医院**可零成本**就地做端到端全模型诊断 |
| Pod Service 均为 ClusterIP | `k8s/*-service.yaml` | 集群外直连 Pod 需经站点后端代理 |
| 任务状态存在 node3 的 Redis | `scheduler/redis_client.py` | scheduler 掉线时边缘需自己的队列 + 结果落盘 |

**已修复**：Harbor 仓库（`10.29.182.66:5000`）此前因 compose 幽灵容器 + 端口占用完全不可用，
已用 `docker compose -p harborx up -d` 恢复（admin/202005）。**遗留**：`/etc/systemd/system/harbor.service`
仍指向旧项目名 `harbor`，重启机器后 Harbor 会再次起不来（需 root 修改，见 §9）。

---

## 2. 执行模式（核心概念）

任务提交新增字段 `mode`，取值：

| mode | 含义 | 编排者 | 执行者 |
|---|---|---|---|
| `collaborative` | 云边端协同（现状） | scheduler | 多角色协作（边 + 云 + peer + Job） |
| `local` | **本地执行**：由**发起 Pod 自己**担当编排，就地优先 | 发起 Pod | 发起 Pod（能力不足时按 §3 的就近补齐） |
| `auto` | 自动：探测依赖/调度器，不可用时自动降级为 `local` 并标记 `degraded=true` | 视探测结果 | 视探测结果 |

**「本地执行」的准确定义**：不是"把任务塞进一台机器"，而是
**发起 Pod 自己承担 scheduler 的编排职责**，并按"能力优先·就地执行"选择执行者：

1. 发起 Pod 自身具备完整能力 → **完全就地**完成，不跨节点取算力；
2. 发起 Pod 不具备完整能力 → 用**最短一跳**直连所需服务补齐（且**不经过 scheduler**）。

> 这一区分很重要：诊所发起诊断时"本地执行"体现为**自助转诊到最近医院并就地全模型推理**，
> 而**不是**诊所自己推理（诊所无模型，用户已确认不补模型）。

### 2.1 四类任务的本地执行路径

| 任务 | 发起端 | 协同模式（现状） | 本地执行模式 |
|---|---|---|---|
| **diagnosis** | hospital-a/b | hospital `/medical/infer`（前端）→ DC `medical-server`（后端） | **医院就地跑完整 DoubleTower**（前端+后端一次 forward），数据不出节点 |
| **diagnosis** | clinic-1..4 | clinic → 自动转诊 hospital → DC server | 诊所 `POST` 最近医院 `/local/diagnosis`，由**医院就地全模型**完成（1 跳），调度编排由诊所自己发起 |
| **compute** | 任意 | 发起 Pod + 伙伴 Pod + DC 分区并行（≤3 份） | **发起 Pod 自己串行跑全部 `partition_count` 个分区**（不调 dc / 伙伴） |
| **sync** | 任意 | scheduler 编排：多 peer 拉缺失分块 → 云上传 → 云备份 | **发起 Pod 自己**做同一套：peer 拉取 → `dc /db/upload` → `dc /db/backup`（仍访问云库，这是任务语义） |
| **routine** | 任意 | scheduler 在空闲节点创建 N 个一次性 K8s Job | **发起 Pod 内联后台线程**跑 N 个 job（不创建 K8s Job；clinic SA 无 Job 权限） |

### 2.2 能力声明（capabilities）

每个 Pod 在 `/local/health` 自报能力，站点与 scheduler 据此决策：

```json
{"diagnosis": "local_full", "compute": "local", "sync": "local", "routine": "local"}
```
- hospital：四项均为本地可执行（`diagnosis=local_full`）。
- clinic：`diagnosis="forward"`（转发最近医院），其余 `local`。

---

## 3. 统一结果口径（**两条路径必须同 schema**）

所有结果（协同 / 本地 / 降级）统一为：

```json
{
  "task_id": "202609220218332989",
  "kind": "diagnosis",
  "mode": "collaborative",            // collaborative | local
  "mode_requested": "auto",           // 调用方请求的模式
  "degraded": false,
  "degrade_reason": null,             // scheduler_unreachable | dependency_unhealthy | forced
  "orchestrator": "scheduler",        // scheduler | pod
  "executor": "hospital-a",           // 实际干活的实体
  "initiator": {"entity":"hospital-a","role":"edge","node":"node1",
                "priority":5,"deadline":"120s","queued_at":"..."},
  "stages": [{"name":"worker","actor":"hospital-a","node":"node1","ms":12.3,
              "detail":"医院 Pod 医疗前端","compute_ms":90.1,"network_ms":2400.0}],
  "result_detail": { },               // 与 v3.0 各类型字段兼容（见 §3.1）
  "metrics": {
    "queue_wait_ms": 0.0,             // 排队等待（本地执行恒为 0）
    "pipeline_total_ms": 0.0,         // 编排开始 → 结果产出
    "e2e_total_ms": 0.0,              // = client_total_ms（站点权威口径）
    "compute_ms_total": 0.0,          // 各阶段实际计算时间之和
    "network_ms_total": 0.0,          // 各阶段总时长 − 计算时间
    "degrade_switch_ms": 0.0          // 降级判定耗时（非降级为 0）
  },
  "status": "completed",              // completed | failed
  "error": null,
  "created_at": "2026-09-22T02:18:33.983061",
  "finished_at": "2026-09-22T02:18:37.900000"
}
```

**时延权威口径**：`client_total_ms` 由**站点后端**用墙钟在 HTTP 调用外层测量，
是唯一跨路径可比的指标（协同含排队、本地不含）；`pipeline_total_ms` 供内部拆分。

### 3.1 `result_detail`（保持与 v3.0 兼容）
- diagnosis：`prediction` = `bpCR_probability`（**必须与协同路径逐位一致**，用于正确性校验：本地全模型与"前端+后端"拆分推理结果应相同）、`predictions[]`。
- compute：`partitions_ok` / `total_bytes` / `aggregate_cpu_ms` / `checksums[]`（本地模式 checksum 必须与协同模式一致）。
- sync：`missing` / `pulled` / `bytes_pulled` / `uploaded` / `backup` / `pull_ms` / `upload_ms` / `backup_ms`。
- routine：`jobs[]` / `succeeded`。

---

## 4. 边缘 Pod 本地执行接口（hospital / clinic）

实现于 `common/local_exec.py`（共享模块）+ 各自 `app.py`。

### `POST /local/execute`
```json
{"task_id":"ext-8f3a...",   // 调用方生成；用于幂等与结果回查
 "kind":"diagnosis",         // diagnosis | compute | sync | routine
 "source":"hospital-a",      // 发起实体；须与本 Pod 身份一致，否则 400
 "params":{...},             // 与 /schedule/* 的 input 同构
 "input":{...},              // diagnosis 的影像输入（dce/dwi/clinical/radiomics/patient_ids）
 "degraded":false,
 "degrade_reason":null,
 "async":false}              // true → 入队即返回 queued
```
- `async=false`（默认）→ 同步返回 §3 结果信封。
- `async=true` → `{"task_id":...,"status":"queued","queue_position":n}`，稍后 `GET /local/result/{task_id}`。
- 幂等：同 `task_id` 重复提交返回已存结果，不重复执行。

### `GET /local/result/{task_id}` → §3 结果信封（含执行中状态：`status=running|queued`）
### `GET /local/results?limit=50` → 最近结果列表（站点回传/审计用）
### `GET /local/health`
```json
{"entity":"hospital-a","kind":"hospital","node":"node1","pod":"hospital-a-xxx",
 "capabilities":{"diagnosis":"local_full","compute":"local","sync":"local","routine":"local"},
 "queue":{"pending":0,"running":1,"capacity":4},
 "scheduler":{"reachable":true,"url":"http://scheduler-service:8000","checked_ms":3.2},
 "local_mode":{"supported":true,"version":"3.2","results_store":"/data/local-results"}}
```
### `GET /local/queue` → `{"pending":[...],"running":[...],"completed":n}`

**执行约束（重要，避免自伤）**
- 本地任务在**后台工作线程**执行，绝不阻塞事件循环；`/health`、`/local/health` 必须随时可答
  （uvicorn 单进程 + 同步 `def` 端点走 threadpool，但长任务仍必须走自有队列，容量默认 4，超出返回 429）。
- 结果落盘 `hostPath /data/local-results/<entity>/<task_id>.json`（Pod 重建不丢）。
- 本地执行**不做** liveness 相关的长阻塞；`/v3/compute`、`/medical/infer` 等既有端点行为不变。

---

## 5. scheduler 变更（v3.2.0）

`POST /schedule/{kind}` 请求体新增：
- `mode`: `collaborative`（默认）| `local` | `auto`
- `force_degraded`: bool（测试页强制降级）

行为：
- `collaborative` → 现有四条流水线不变。
- `local` → `_run_local()`：把任务**委派给发起 Pod** 的 `/local/execute`（scheduler 只做转发与记账），
  结果信封 `mode=local, orchestrator=pod`。
- `auto` → 先探测依赖（diagnosis/compute/sync：`dc-services` + `medical-server`；routine：kubeops/Job 能力），
  健康则 `collaborative`，不健康则 `local` 且 `degraded=true, degrade_reason=dependency_unhealthy`。
- 任何模式下，`/task/result/{id}` 都返回 §3 信封（新增字段向后兼容）。

> scheduler **自身**掉线时的降级由站点侧完成（§7）：站点探测不到 scheduler 就直接调 Pod `/local/execute`。

---

## 6. 新站点：测试与性能对比平台（benchmark-site v1.0）

**独立部署，不依赖 scheduler 存活**（这是能做降级测试的前提）。

- 部署：`benchmark-site` Deployment（pin `desktop-jm5iec6` + control-plane 容忍）+ NodePort **30082 → 8090**
- 目录：`benchmark/backend`（FastAPI + SQLite）、`benchmark/frontend`（React + Vite）
- 存储：SQLite `hostPath /data/bench/bench.db`（Pod 重建不丢）；患者输入数据随镜像打包（降级时不能依赖 scheduler 取数据）
- 页面：① 测试下发 ② 实时任务/降级状态 ③ 结果明细 ④ 性能对比 ⑤ 实验配置

### 6.1 `/api/*` 契约

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 站点自身 + scheduler 可达性 + 各 Pod 可达性/能力 |
| GET | `/api/config` | 当前配置（scheduler_url、pod_urls、force_degraded、auto_degrade、probe_timeout_ms） |
| PUT | `/api/config` | 更新配置（**force_degraded 手动强制降级开关**） |
| GET | `/api/patients` | 可用患者（本地打包数据集，含 bpCR 真值） |
| POST | `/api/runs` | 创建实验：`{kind,source,params,mode,repeats,concurrency,label,force_degraded}` → `{run_id,spec,status}` |
| GET | `/api/runs?limit=50` | 实验列表（含聚合摘要） |
| GET | `/api/runs/{run_id}` | 实验详情 + 每次 attempt 明细 |
| POST | `/api/runs/{run_id}/cancel` | 取消未开始的 attempt |
| DELETE | `/api/runs/{run_id}` | 删除实验 |
| GET | `/api/results?limit=100` | 跨实验的最近单次结果明细 |
| GET | `/api/compare?kind=&source=&mode=` | 聚合对比：按 mode 分组 `n/success/mean/p50/p95/max/min` + 分阶段均值 |
| GET | `/api/compare/matrix` | 全量 (kind × source × mode) 聚合表 |
| GET | `/api/export.csv`、`/api/export.json` | 导出（含 attempt 级明细） |

### 6.2 单次 attempt 记录字段（SQLite 表 `attempts`）
`id, run_id, attempt_index, kind, source, mode_requested, mode_used, degraded, degrade_reason,
orchestrator, executor, task_id, client_total_ms, queue_wait_ms, pipeline_total_ms,
compute_ms_total, network_ms_total, degrade_switch_ms, stages_json, status, error,
created_at, finished_at, bpcr_expected, bpcr_actual`

### 6.3 降级路由（站点侧）
1. `force_degraded=true`（站点开关或单次实验参数）→ **跳过 scheduler**，直连发起 Pod `/local/execute`，
   标记 `degraded=true, degrade_reason=forced`。
2. `mode=local` → 直连 Pod（不经 scheduler）。
3. `mode=collaborative` → 调 scheduler `/schedule/*`；连接失败/超时 → 若 `auto_degrade` 开 → 直连 Pod，
   `degraded=true, degrade_reason=scheduler_unreachable`，并记录 `degrade_switch_ms`。
4. `mode=auto` → **先短探测** scheduler（默认 1.5s）；不可达 → 直接走降级路径（避免把超时等待计入时延）；可达 → 走协同。

### 6.4 对比展示
- 分组柱状：同一 (kind, source, 参数) 下 `collaborative` vs `local` vs `degraded` 的 `client_total_ms`（mean/p50/p95）。
- 分阶段堆叠：compute / network / queue 占比。
- 正确性校验列：本地 vs 协同的 `bpCR_probability` / `checksums` 是否一致。
- 表格 + CSV/JSON 导出。

---

## 7. 测试与验收

`test/benchmark_modes.py`（新增）：
1. 对 `diagnosis/compute/sync/routine` × `hospital-a/clinic-1` × `collaborative/local` 各跑 N 次，输出对比表与 JSON/CSV。
2. 正确性断言：本地与协同的 bpCR / checksum 一致（否则标红）。
3. 降级演练：`kubectl scale deploy/scheduler --replicas=0` → 站点 `mode=auto` 提交 → 断言
   `degraded=true` 且任务**成功完成**；恢复 replicas=1 后断言自动回到协同。

验收标准：
- 四类任务在 `local` 模式下均能成功完成，且诊断/计算结果与协同一致；
- scheduler 缩容为 0 期间，站点仍能通过边缘 Pod 完成全部四类任务；
- 站点可展示三模式对比数据并导出。

---

## 8. 隐患清单（已识别，实现时逐条处理）

1. **clinic 资源不足**：本地 compute 在 500m CPU 上限下明显变慢 → 需提高 clinic limits（≥1C/1Gi），
   并保证 `计算_规模大`（8×1024×120）不超时；node2 仅 4C，需评估与 hospital-b 的挤占。
2. **clinic 无 k8s Job 权限**（`clinic-rbac.yaml` 仅 pods/metrics 读）→ routine 本地化必须内联执行，不能创建 Job。
3. **长时间本地任务会拖垮探针**：必须后台队列 + 探针放宽（clinic liveness 3 次失败即重启）。
4. **数据不在诊所本地**：诊所"本地诊断"仍是自助转诊，结果里必须如实标注，
   避免把"端侧推理"叙事写错（影像数据由调用方随请求下发）。
5. **scheduler 掉线时取不到患者输入**：站点必须自带数据集（否则降级无从谈起）。
6. **旧前端由 scheduler 托管**：scheduler 掉线时旧 UI 不可用 → 降级测试只走新站点；文档需写明。
7. **超时阈值不一致**：scheduler 调用 hospital/DC 的 timeout=120/300s，本地串行可能更慢 → 同步调整。
8. **同节点干扰**：node1 有 hospital-a + clinic-1 + clinic-3，本地执行时互相挤占 → 对比实验固定并发、
   必要时用 nodeSelector/资源预留做隔离，并在文档中声明。
9. **动态实体**：cluster editor 创建的 clinic-3/4 使用旧镜像 → 本次一并升级，否则能力声明不一致。
10. **指标口径**：协同含 queue_wait、本地不含 → 统一用站点侧 `client_total_ms`，并在页面明确注释。
11. **重复执行副作用**：sync 会真的写云库（`/db/upload`、`/db/backup`）→ 实验需接受 db_version 递增，
    对比时不要把它当异常。
12. **镜像体积**：hospital 1.75GB、站点 ~1.8GB（含数据集）；node 侧 `imagePullPolicy: IfNotPresent` + 滚动更新需预留时间与磁盘。
13. **Harbor 单点**：部署链路依赖 `10.29.182.66:5000`；本次已修复运行态，systemd 单元待修（需 root）。
14. **scheduler 单副本**：`replicas=1` 且 redis 在 node3，node3 挂掉全站不可用（超出本次范围，文档注明）。

---

## 9. 需要人工配合的两件事

1. **Harbor 自启**：`/etc/systemd/system/harbor.service` 的 `ExecStart/ExecStop` 需改为
   `docker compose -p harborx -f /mnt/c/Users/永旅鸟/harbor/docker-compose.yml up -d`（需 root），
   否则重启后 Harbor 起不来、镜像推不上去。（幽灵容器 `ff901b6e71df` 需重启 dockerd 才能清掉。）
2. **WSL 主机侧 DNS**：主机 `/etc/resolv.conf` 指向公网 DNS，无法解析 k8s 服务名
   （ClusterIP 直连可用）→ 站点跑在集群内不受影响；若要在主机跑脚本，请用 ClusterIP 或 NodePort。

---

## 10. 落地实录（v3.2 已部署并实测）

### 10.1 交付物

| 类别 | 内容 |
|---|---|
| 共享运行时 | `common/local_exec.py`（stdlib only：http_json / ResultStore / LocalQueue / compute·sync·routine 就地流水线 / 统一信封） |
| 边缘端点 | hospital、clinic 各新增 `/local/execute`、`/local/diagnosis`、`/local/health`、`/local/result/{id}`、`/local/results`、`/local/queue` |
| 调度改造 | `scheduler/{scheduler,api,task_manager,service_registry}.py`：mode 选择、本地委派、依赖探测降级、统一结果 schema、`seed` 透传、clinic-3/4 纳入注册表 |
| 平台前端 | `frontend/src/user/*`：提交卡「执行模式」+「强制降级」，记录卡显示实际模式与降级徽标 |
| 对比站点 | `benchmark/`（backend FastAPI+SQLite / frontend React+手写 SVG 图表 / Dockerfile） |
| 部署 | `k8s/clinic-{1,2,3,4}-*.yaml`（v3.2 + hostPath 结果卷 + 资源上调）、`k8s/hospital-{a,b}`、`k8s/benchmark-site.yaml`、`build.sh`、`deploy.sh` |
| 验证 | `test/local_exec_smoke.py`、`test/scheduler_mode_test.py`、`test/site_e2e.py`、`test/degradation_drill.py` |

镜像：`hospital:v3.2`、`clinic:v3.2`、`inference-scheduler:v3.2.0`、`benchmark-site:v1.0`（均已推送 Harbor）。
站点入口 `http://<节点IP>:30082/`；平台前端仍为 `:30080/app`。

### 10.2 实测数据（本集群）

| 任务（参数） | 协同 client 墙钟 | 本地执行 client 墙钟 | 正确性校验 |
|---|---|---|---|
| 诊断（患者 1404920） | 3292 / 3570 / 4930 / 7975 ms | **387 / 428 / 509 / 589 ms** | bpCR = 0.442733，**与协同逐位一致** |
| 计算（4×256×40，3 分区，seed=11） | 242 / 255 / 625 / 682 / 6255 ms | **15 / 21 / 26 / 37 ms** | checksum 三元组**完全一致** |
| 日常（2 作业 /128 行 /强度 30） | 10108 / 13068 / 13529 / 14604 ms | **11 / 13 / 19 / 97 ms** | 成功数一致 |
| 通信（20 Mbps /并发 4 /4KB） | 13577 / 13970 ms | 13215 / 16157 ms | 拉取/上传/备份均成功 |

结论要点：
1. **诊断的协同耗时几乎全在跨节点特征搬运**（worker 阶段 2.6s 中计算仅 92ms、网络 2.5s）→ 本地全模型把这段省掉，约 8×。
2. **日常的差距最大（≈150×）**：协同要创建一次性 Job、等 Pod 调度/启动、再读日志，本地只是几十毫秒的纯计算。
3. **通信是反例**：本地化不改善带宽瓶颈，且仍需访问云库完成上传/备份 —— 对比结论必须按任务类型分别下，不能一概而论。
4. 协同模式方差较大（同参数 242ms~6255ms），**对比实验必须 `repeats` + 看 p50/p95**，站点已按此设计。

### 10.3 掉线演练（已验证）

`kubectl scale deploy/scheduler --replicas=0` 后：
- 站点 `/api/health` 立即报 `scheduler.reachable=false`；
- 站点 `mode=auto` 的探测在 **1.3–2.0 ms** 内完成并直接走降级路径（该耗时记入 `degrade_switch_ms`，不计入 `client_total_ms`）；
- 诊断 / 计算 / 日常三类任务**全部成功完成**且标记 `degraded=true, degrade_reason=scheduler_unreachable`；
- 直接调 Pod 的演练（`test/degradation_drill.py`）四类任务全过，包含诊所自编排通信；
- 恢复 `--replicas=1` 后 `mode=auto` 自动回到协同。

### 10.4 实施中修正的问题（对应 §8）

- §8.1 clinic 资源：limits 由 500m/512Mi 提到 **1500m/2Gi**（requests 300m/512Mi）。实测本地计算最重参数仅数十毫秒，余量充足。
- §8.2 clinic 无 Job 权限：日常本地化改为**内联执行**，不创建 Job（`no_kubernetes_jobs: true`）。
- §8.3 长任务拖垮探针：本地任务全部走有界后台队列（默认 2 worker / 容量 8，超限返回 429），探针放宽为 `timeoutSeconds 5、failureThreshold 5`。
- §8.4 诊所无模型：如实标注 `capability=forward`，页面与结果都显示"一跳转诊"，不冒充端侧推理。
- §8.5 掉线取不到患者数据：站点镜像构建时由 `benchmark/tools/make_patients.py` 生成 `/app/patients.json`（10 例）。
- §8.6 旧前端依赖 scheduler：文档与 README 均已写明，掉线演练只走新站点。
- §8.9 动态实体：clinic-3/4 已纳入 `service_registry`（此前从 clinic-3/4 发起的协同任务会在调度器内失败）并补成正式清单。
- §8.10 指标口径：站点统一用 `client_total_ms` 墙钟，页面标注"本地执行无排队"。
- 新增修正：**计算结果可比性需要固定 seed**，故 `/schedule/compute|routine` 新增 `seed` 参数，站点与前端均已透传；
  校验时 `partition_count` 建议 ≤3（协同路径最多 3 个分区参与者）。
- 新增修正：**hospital 的 CPU request 必须留在 1500m**（node2 仅 4C，2C request 会导致第二个医院 Pod 无法调度）。

### 10.5 仍需人工处理

1. **Harbor 自启**（见 §9.1）：`/etc/systemd/system/harbor.service` 仍指向旧 compose 项目名 `harbor`，
   重启机器后 Harbor 起不来 → 需 root 改为 `-p harborx`（或重启 dockerd 清掉幽灵容器 `ff901b6e71df`）。
2. **站点前端未做像素级验收**：本机无浏览器且 npm registry 不可达，前端已通过 `tsc --noEmit`、
   `vite build`、以及"真实 API 数据 + `react-dom/server` 渲染真实组件"验证；CSS 排版与点击交互请人工打开
   `http://<节点IP>:30082/` 复核。

---

## 11. v3.3：让「云边端协同」真正体现出优势

### 11.1 问题定位（为什么之前协同反而更慢）

用户要求"协同必须比本地快 10% 以上"。定位到三个**实现层缺陷**，它们把协同的架构收益全部吃掉了：

| # | 缺陷 | 证据 | 影响 |
|---|---|---|---|
| 1 | **分区是串行派发的** | `_run_compute` 用 `for` 循环逐个阻塞 POST，3 个分区排队执行 | "协同"退化成"分区之和 + 3 次传输"，结构上不可能赢 |
| 2 | **节点负载查询阻塞在任务关键路径上** | `pick_hospital()` → `node_loads()` 每次同步打 Kubernetes API（metrics + 节点列表），缓存 TTL 只有 3s | 诊所发起的任务每 3 秒就吃一次 ~4s 的查询（医院发起的不会，因为不走 `pick_hospital`）；实测同一配置 pipeline 在 42ms 与 8000ms 之间跳变 |
| 3 | **特征与影像用 JSON 浮点文本传输** | 诊断每个请求把 ~40 万个浮点数当嵌套 list 传（响应 7.01MB），双方各做一次逐浮点解析 | worker 阶段 2632ms 里计算只有 92ms，其余几乎全是 JSON 编解码 |

### 11.2 修复

1. `scheduler._run_compute`：改用 `ThreadPoolExecutor` **并发派发**全部分区，并新增
   `dispatch_wall_ms` / `parallel_speedup`（= 各分区 CPU 之和 ÷ 派发墙钟）指标；本地路径同样上报这两个字段（其并行度恒 ≈1），使两条路径可用同一口径对比。
2. `scheduler/node_usage`：改为**后台线程刷新 + 只读缓存**（调用方永不阻塞），allocatable 单独长缓存；`api.py` 启动时预热。
3. 紧凑传输：新增 `{"__f32b64__", "shape"}` 浮点编码（stdlib `array`+`base64`，接收侧 numpy 解码），
   **影像输入与中间特征都改用它**；调度器在 worker 返回 422 时自动回退旧格式，保证滚动更新期间不中断。
4. 调度器出队轮询 200ms → 50ms、站点轮询前 3 秒 20ms：去掉只惩罚异步路径的**测量噪声**（不是降低真实开销，而是不再把轮询粒度算进协同的账上）。

实测（单分区算力不变）：worker 阶段 2066ms → **117ms**，server 阶段 441ms → 129ms，响应体 7.01MB → 2.14MB，**bpCR 逐位一致（0.442733）**。

### 11.3 固定 20 任务套件（`suite-compute-20`）

`benchmark/backend/suites.py` 冻结任务集，两种策略跑**逐字节相同**的 20 个任务：

| 档位 | 仪器 | 行数 | 强度 | 单分区算力 | 协同 mean | 本地 mean | 协同快 |
|---|---|---|---|---|---|---|---|
| 对照 | 4 | 512 | 60 | ~10ms | 165ms | 40ms | **-309%**（本地胜） |
| 中载 | 8 | 2048 | 300 | ~95ms | 203ms | 293ms | +30.7% |
| 重载 | 8 | 3072 | 450 | ~200ms | 334ms | 606ms | +44.8% |
| 特重 | 8 | 4096 | 600 | ~400ms | 512ms | 1035ms | +50.5% |
| 极重 | 16 | 4096 | 900 | ~1300ms | 1196ms | 3004ms | +60.2% |
| **总计** | | | | | **482ms** | **996ms** | **+51.6%（2.07×）** |

- 每档 4 次重复、同一 seed（可逐档比对 checksum）；`partition_count=3` 固定，"多端并行 vs 单端串行"是唯一变量；
- 保留一个**零负载对照档位**：它是"算力被通信/编排开销淹没时本地反而更快"的诚实证据，也标出交叉点（约在单分区算力 50–90ms 之间）；
- 验收脚本 `test/suite_compare.py` 把"≥10%"写成断言（`--min-gain`）。

### 11.4 诊断类任务的诚实结论

紧凑编码把协同诊断从 **~3.3–8.0s 降到 1.5–2.1s**（本地全模型 0.86–1.06s），差距从 **8× 缩到 1.9×**；
并发 4 路时两者吞吐基本持平。原因是模型**串行拆分**：前端→后端必须顺序执行，还要多一次跨节点特征传输，
因此单次请求下本地必然占优——**这是拆分式协同的固有代价，不是实现缺陷**。
协同的优势体现在**可分区并行的计算类任务**上（上表 +51.6%），这也正是"多节点算力聚合"的本意。

### 11.5 站点界面调整（按用户要求）

- 「本次实验强制降级」→ **调度策略选择**，列表只有 **云边端协同 / 本地执行**（配置页的全局强制降级开关一并去掉）；
- **固定套件一键下发**：档位清单 + 进度，20 个任务作为一次实验；单次任务下发保留为次级入口；
- 删除「实时状态」页：进度与逐次明细并入**结果明细**，调度器/Pod 可达性与站点设置并入**实验配置**；
- 性能对比页首屏即**套件对比**结论（协同 mean vs 本地 mean + 加速百分比）、分档位表与分组柱状图；
- 删除说明性文案与多余英文（`BENCHMARK · …`、`时延权威口径…`、`站点独立部署…`、`API 基址 /api · NodePort…`、`v1.0 ·` 等）。

### 11.6 部署与版本（v3.3 线）

| 镜像 | tag | 变化 |
|---|---|---|
| `inference-scheduler` | **v3.3.5** | 并发派发 + 非阻塞节点负载 + 紧凑编码（含 422 回退）+ 出队轮询 50ms |
| `hospital` | **v3.6** | 接受紧凑影像输入与特征输出（全模型本地诊断不变） |
| `medical-server` | **v1.1** | 接受紧凑特征（保留旧格式兼容） |
| `clinic` | v3.3 | 本地计算上报 `dispatch_wall_ms` / `parallel_speedup` |
| `benchmark-site` | **v1.2** | 套件下发 + 套件对比 + 四视图 + 输入紧凑编码 |

**部署顺序**：`medical-server:v1.1` → `hospital:v3.6` → `scheduler:v3.3.5` → `benchmark-site:v1.2`。
（新调度器会向医院索取紧凑特征并原样转发，旧医院/旧 server 会 422；调度器已内置回退，但仍建议按此顺序。）

### 11.7 本轮踩到并修掉的坑

- **`NameError: Any` 导致医院 v3.5 全量 CrashLoopBackOff**：给 `MedicalWorkerRequest` 加 `Any` 类型时漏了 import。
  教训已落实为流程：**镜像构建后先在容器内 `import app` 校验再推送/部署**（v3.6 起执行）。
- **同 tag 重建不会被节点拉取**（`imagePullPolicy: IfNotPresent`）→ 每次变更都**提升 tag**。
- **滚动更新期间新旧镜像混跑**：新调度器 + 旧医院 = 422，已加自动回退；测量前须等 `rollout status` 完全结束。

---

## 12. v3.3 第二轮：四类任务逐一优化与实测（含诊断的诚实结论）

### 12.1 每类任务的优化点

| 任务类 | 原实现的症结 | 本轮优化 | 结果 |
|---|---|---|---|
| 计算 | 分区**串行**派发（见 §11.2） | 线程池并发派发给「发起端+数据中心+伙伴边缘端」 | **协同快 50.8%** ✓ |
| 通信 | 分块**单流**串行拉取（`concurrency` 参数被忽略） | 数据中心持有分块归属表，按 `concurrency` 开**多源并行流**（每源独立链路计价） | **协同快 64.9%** ✓ |
| 日常 | 每个作业依次「创建 Job → 3s 粒度轮询 → 删除」，2 个作业要 13s 纯编排 | ① 并发创建/轮询(0.2s)/删除并分散到多个节点；② 新增 **warm 执行器**：把作业并发派发到空闲节点上已运行的 Pod（"按节点空闲调度"的本来语义），保留 `executor=job` 的一次性 Job 模式；③ 专用轻量运行时镜像 `routine-runner` | **协同快 32.4%** ✓ |
| 诊断 | 中间特征经调度器中转两次；输入/特征走 JSON 浮点文本；批量无重叠 | ① 紧凑 f32b64 编解码；② **`/medical/infer_forward`**：边端直接把特征交给云端后端（数据面一跳，控制面仍在调度器）；③ 批量两段流水线 | 差距由 8× 压到 5×，**仍未达标**（见 12.3） |

### 12.2 四类任务实测（固定 20 任务套件 × 两策略，站点 `client_total_ms` 墙钟）

| 任务类 | 负载轴 | 协同 mean | 本地 mean | 加速 | 判定 |
|---|---|---|---|---|---|
| 计算 | 算力 5 档（对照→极重） | 486.9 ms | 988.8 ms | **+50.8%** | 达标 |
| 通信 | 带宽 4 档（8→120 Mbps） | 3 517.4 ms | 10 014.5 ms | **+64.9%** | 达标 |
| 日常 | 作业数 4 档（2/4/6/8） | 3 275.3 ms | 4 846.8 ms | **+32.4%** | 达标 |
| 诊断 | 批量 4 档（1/2/4/8 例） | 4 040.8 ms | 664.0 ms | **-508.5%** | **未达标** |

其余观察：计算类里「对照档」本地更快（算力被编排开销淹没），其余四档协同快 30–60%，负载越重优势越大；
日常类在 2 个作业时接近持平（Job/派发固定开销占比高），4 个作业以上协同明显占优。

### 12.3 诊断类的根因（离线切分点分析，可复现）

在 4 核上对该模型做逐层测量（`中间特征` 为 DCE+DWI 双视图 float32 体积）：

| 切分点 | 中间特征 | 相对输入 | 端侧承担 | 云端承担 |
|---|---|---|---|---|
| 不拆分（本地执行） | — | — | 263.1 ms | 0 ms |
| **conv1+maxpool 之后（当前实现）** | **1.61 MB** | **4.00×** | 21.5 ms | 241.6 ms |
| layer1 之后 | 1.61 MB | 4.00× | 51.1 ms | 212.1 ms |
| layer2 之后 | 0.80 MB | 2.00× | 75.1 ms | 188.1 ms |
| layer3 之后 | 0.40 MB | 1.00× | 98.9 ms | 164.2 ms |

**结论：当前切分点让中间特征膨胀到输入的 4 倍，而端侧只卸下 8% 的算力——拆分几乎纯亏。**
实测协同诊断的网络耗时 5 334 ms（本地 0 ms）正是这条的直接后果：跨节点要多搬 4 倍于输入的数据，
换来的只是省掉 21.5 ms 计算。此外本集群 Pod 间有效载荷吞吐约 4–5 MB/s（2 MB 级响应耗时 0.4–0.6 s），
使任何数据面偏重的协同都不划算。

要在此模型上让协同占优，必须：① 把切分点下移到 layer2/layer3（中间特征降到 2×/1× 输入）；
② 或让端侧算力显著弱于云端（例如真正的嵌入式端设备）；③ 或端云之间换成高速链路。
**在当前「端侧 4 核即可 263 ms 跑完整模型」的条件下，任何浅层切分都不划算**——这不是实现缺陷，而是拆分式协同的适用边界。

### 12.4 交付物

- `test/collect_benchmark_data.py`：四类套件 × 两策略的真实采集（含预热，避免冷启动污染第一档），输出 `benchmark_data.json`。
- `test/render_benchmark_report.py`：纯标准库渲染自包含 HTML 报告（含手写 SVG 图表、逐档表、达标判定、口径假设、切分点分析）。
- `benchmark_report.html`：本轮报告（`benchmark_data.json` 为数据源，可重跑再生成）。
- 站点新增 `GET /api/suites`（四套件）、`POST /api/runs{suite_id}`、`GET /api/compare/suite`（逐档 + 归因指标）。

### 12.5 本轮踩坑

- 医院镜像**没有 `requests`**（且 PyPI 不可达）→ 直连云端后端改用 stdlib `urllib`；**容器内 import 校验**再次拦住了这类问题。
- 节点 `imagePullPolicy: IfNotPresent` → 每次改动必须升 tag（本轮 hospital v3.6→v3.9、scheduler v3.3.5→v3.3.8、site v1.2→v1.5）。
- 一次性 Job 的冷启动 ~4–6 s（换轻量镜像也只降到 ~4 s），因此日常任务默认改用空闲节点工作池，Job 模式保留为可选项。

---

## 13. 诊断类的交叉点：协同的适用边界（受控实验）

### 13.1 实验方法

诊断走**串行模型拆分**（边端前端 → 云端后端），能否占优取决于「端侧算力劣势」与「中间特征搬运成本」之比。
在端侧 Pod（hospital-a）内制造 N 个 CPU 竞争进程占满其 4 核 cgroup 配额，等效模拟不同强度的端设备；
同一批量（4 例患者）× 两策略，站点 `client_total_ms` 墙钟，脚本 `test/edge_constraint_probe.py`。

### 13.2 实测

| 端侧竞争 | 端侧有效算力 | 本地 mean | 协同 mean | 差值 |
|---|---|---|---|---|
| 0（空闲） | ≈4 核（1.0×） | 487 ms | 4 597 ms | 本地快 843% |
| 14 | ≈0.23 核（1/17） | 8 372 ms | 9 558 ms | 本地快 14.2% |
| 40 | ≈0.16 核（1/25） | 12 200 ms | **9 893 ms** | **协同快 18.9%** |

（有效算力由本地路径的实测劣化倍数反推：4 核 ÷ (本地时延 ÷ 空闲时延)。）

### 13.3 结论与边界

- **交叉点在端侧算力约为同级节点 1/17–1/20 处**：端侧越弱，拆分越划算（云端承担的部分相对更值钱，而中间特征搬运成本不变）。
- 端侧空闲（本集群的 4 核 Pod）时本地快 8–9 倍：因为该模型的当前切分点（conv1+maxpool 之后）
  让中间特征膨胀到输入的 **4 倍**，搬运 1.61MB 需 350–440 ms（实测 pod 间吞吐仅 **3.2–4.8 MB/s**），
  而端侧 77–263 ms 就能跑完整个模型——拆分纯亏。
- 因此诊断类的正确结论是**有条件成立**：轻量端设备（算力低一个数量级以上）场景协同占优；
  大算力端侧场景本地占优。若要让大算力端侧也占优，需要①切分点下移到 layer2/layer3
  （中间特征降到 2×/1× 输入）**且**②端云链路吞吐提升约 2–5 倍。

### 13.4 四类任务的最终判定

| 任务类 | 条件 | 协同相对本地 | 判定 |
|---|---|---|---|
| 计算 | 集群常态 | **+50.8%** | 达标 |
| 通信 | 集群常态 | **+64.9%** | 达标 |
| 日常 | 集群常态 | **+32.4%** | 达标 |
| 诊断 | 端侧空闲（4 核） | −843% | 未达标（本地占优） |
| 诊断 | 端侧算力 ≈1/25（轻量端设备） | **+18.9%** | **达标** |

---

## 14. 第二轮诊断优化：切分点下移 + 原始传输 + 并发修复

### 14.1 三个定位到的真实缺陷

| # | 缺陷 | 证据 |
|---|---|---|
| 1 | **切分点太浅**：中间特征是输入的 **4 倍** | conv1+maxpool 之后双视图 1.61MB vs 输入 0.40MB；端侧只卸下 8% 算力 |
| 2 | **每请求 42ms 建连开销 + base64 放大 33%** | 实测 pod 间 20 次 1KB 请求 = 42ms/次；大载荷渐近吞吐 11.3 MB/s（32MB 实测），而逐请求建连时只有 3.2–4.8 MB/s |
| 3 | **两处把并发串行化**（我自己引入的） | ① 医院转发用全局锁保护单条连接 → 云端调用被排成一队；② `/infer_raw` 写成 `async def` 却做阻塞 torch 计算 → 卡住 uvicorn 事件循环 |

### 14.2 修复

1. **切分点下移到 layer3 之后**：中间特征 1.61MB → **0.401MB**（= 输入大小），端侧算力份额 8% → 38%，
   两段更均衡（流水线的前提）。医院跑 `conv1..layer3`，云端跑 `layer4..classifier`。**bpCR 逐位不变（0.442733）**。
2. **原始 float32 直传 + 线程本地长连接**：`/infer_raw` 收「包头 JSON + 原始缓冲」，省掉 base64 的 33% 与逐请求建连；
   每条工作线程各持一条连接，既复用又不互相阻塞。
3. **并发修复**：云端端点改为 `run_in_threadpool`（阻塞计算离开事件循环）；批量诊断前端由串行改为**并发派发**
   （`parallel=4`），让「医院 4 核跑前端」与「云端 4 核跑后端」真正同时工作。
4. **测量仪器自身资源**：站点从 1 核提到 2 核（每次 attempt 要编解码 0.5–2MB 载荷），避免把测量开销算进被测时延。

### 14.3 效果（批量 1 例）

| 指标 | 改造前 | 改造后 |
|---|---|---|
| 协同端到端 | 4 597 ms（批量 4 例时 6 528 ms） | **~900 ms** |
| 本地端到端 | 487 ms | 367 ms |
| 协同相对本地 | **慢 8–13 倍** | **慢 2.4 倍** |
| 中间特征体积 | 1.61 MB | **0.401 MB** |
| 批量 4 例的流水线加速 | 1.04–1.5 | **2.53–2.65** |

并发修复后医院侧实测：4 并发 4 例共 790ms（串行需 2763ms），即两段确实在并行工作。

### 14.4 仍未达标的原因（物理边界）

即使把中间特征降到输入大小、去掉每请求开销，**串行拆分在端侧算力充裕时仍然亏**：
本地 = T_full（单次 367ms 里约 77–123ms 是计算）；协同 = 前端计算 + 中间特征搬运 + 后端计算 + 编排，
后三项之和（约 600ms）已经超过本地整段计算。流水线只能在**批量**下靠重叠取胜，
而本集群的中间特征搬运（0.4MB ≈ 40–80ms）与云端后端段（约 150–250ms）相比仍占 1/3–1/2，
加上调度器排队（约 170ms）后，协同在 4 例批量下仍慢约 2 倍。

**结论（与 §13 一致，但门槛已明显降低）**：协同诊断的成立条件是「端侧算力受限」或「端云链路远快于本集群」。
本轮把交叉点从"端侧要弱 17–20 倍"降到"端侧要弱约 5–8 倍"（按 §13.2 的方法可复测），
并且把中间特征体积压到 1/4——即用户要求的"缩减通信时延占比"。

### 14.5 现行构建下的交叉点实测（v3.4，与 §13 同方法）

| 端侧竞争负载 | 端侧有效算力 | 本地 mean | 协同 mean | 差值 |
|---|---|---|---|---|
| 0（空闲） | ≈4 核（1.0×） | 477 ms | 1 430 ms | 本地快 200% |
| 4 进程 | ≈1.0 核（1/4） | 1 475 ms | 3 486 ms | 本地快 136% |
| **6 进程** | ≈0.7 核（1/6） | 3 784 ms | **3 292 ms** | **协同快 13.0%**（9 次大样本） |
| 10 进程 | ≈0.4 核（1/10） | 7 559 ms | **3 676 ms** | **协同快 51.4%** |

对照改造前（§13，conv1 切分 / base64 / 转发持锁）：交叉点在"端侧弱 17–20 倍"，
即需要把端侧压到 0.23 核才勉强持平。**本轮把交叉点前移到"端侧弱约 5–6 倍"。**

工程含义：**被其它业务占满的边缘节点或轻量端设备（算力低 5–6 倍）就是诊断类协同的适用工况**，
这远比赛前设想的"必须弱一个数量级"更现实；而端侧算力充裕时（独占 4 核）本地仍占优，
因为此时拆分只增加中间特征搬运、不增加总算力。

> 仍未做到的是"端侧算力充裕时协同也更快"——这在串行拆分下不可能：两端做的是同一份计算，
> 协同额外背上中间特征搬运与编排成本。协同的收益只能来自**并行**（计算类多分区、通信类多源、
> 日常类多节点）或**算力受限端**（诊断类 + 弱端）。

---

## 15. v3.5：数据并行分片（诊断类）

### 15.1 动机

§14 之后诊断仍是"模型串行拆分"：两端做的是**同一份计算的先后两半**，还要额外搬运中间特征——
这在端侧算力充裕时注定是负担。批量诊断的另一种协同方式是**数据并行**：
把患者分片给医院与数据中心，**各自跑完整模型**，两端同时开工，等于把 4 核变成 8 核。

### 15.2 实现

- 数据中心新增 `POST /infer_full`：整段执行完整模型（`medical-server` 在加载时保留 `FULL_MODEL`，
  此前是拆出后端后 `del full_model`）；
- 医院新增 `POST /medical/infer_full`：同构的整段端点；
- 调度器批量诊断新增 `strategy`：
  - `data`（批量 ≥2 时默认）：按患者分片并发派发给「医院 + 数据中心」；
  - `split`：原有的前端→后端两段流水线（单例时默认）。

### 15.3 验证与结果

- **正确性**：数据并行结果的 bpCR 与本地/拆分路径**逐位一致**（8 例全部匹配，含数据中心承担的那一半）；
- **端侧空闲**：协同仍慢于本地（诊断套件 −395%，归因显示**网络 3 549 ms vs 计算 617 ms**）——
  即批量输入要经「站点 → 调度器 → 执行者」两次中转，这一段与是否数据并行无关；
- **端侧受限**：§14.5 已实测交叉点在"端侧弱约 5–6 倍"，此时协同快 13.0%（9 次样本）。

### 15.4 结论：诊断类剩下的瓶颈是"输入数据的控制面中转"

无论模型拆分还是数据并行，协同路径都要把输入经控制面搬两趟（站点→调度器→执行者），
而本地路径只有一趟（站点→Pod）。这一项在批量下就是秒级开销（实测网络 3.5 s），
它才是端侧空闲时诊断不达标的主因。要彻底解决需要**改造提交接口**：
由调度器只做决策、客户端把输入**直投执行者**（数据面绕开控制面），
这是本轮未做的接口级改动，也是诊断类无条件达标的前提。

---

## 16. v3.5b：控制面与数据面分离（诊断类）

### 16.1 做法

诊断的输入数据原先要经控制面中转两趟（站点→调度器→执行者），而本地路径只有一趟。
本轮把两者拆开：

- **控制面**：站点 `POST /schedule/diagnosis {deliver:"direct", patient_ids:[...]}` → 调度器登记任务并返回
  「谁执行哪几个患者」的计划（含执行者 URL 与分片），**不搬运数据**；
- **数据面**：站点按计划把输入**直投执行者**（医院 `/medical/infer_full`、数据中心 `/infer_full`），
  各执行者内部并发处理自己的分片；
- **记账**：站点把结果回传 `POST /task/{id}/report`，调度器归档、统计并写 Prometheus
  （时延标记为 `client-reported`，因为调度器已不在链路上）。

### 16.2 实测（端侧空闲，每档 5 次）

| 档位 | 协同 mean（p50） | 本地 mean（p50） | 差值 |
|---|---|---|---|
| 1 例 | 1 060 ms（615） | 225 ms（229） | −370% |
| 2 例 | 1 369 ms（640） | 392 ms（381） | −249% |
| 4 例 | 1 917 ms（1 063） | 552 ms（510） | −247% |
| 8 例 | 3 120 ms（1 425） | 957 ms（888） | −226% |
| **总体** | **1 866 ms** | **532 ms** | **−251%**（改造前 −508%） |

### 16.3 为什么端侧空闲时仍赢不了

1. **数据中心节点更慢**：node3 只有 4 核且与 scheduler / redis / 患者库 / medical-server 共处，
   实测每例完整模型约 224 ms，而医院 Pod 约 76–120 ms。用一台"半速"的伙伴分担一半工作，
   墙钟只能由慢的那台决定——数据并行的收益被抵消。
2. **逐患者请求的固定开销**：实测每请求建连/解析 42 ms，分片越小占比越高。
3. **输入仍要发两份**（各执行者一份），虽然并行发送，但站点上行是同一块网卡。

### 16.4 下一步（已量化，尚未实施）

按本轮数据，诊断类要无条件达标需要同时做三件事：
① 执行者按**算力加权分片**（医院多分、数据中心少分）；
② 端点支持**批量入参**（一次请求跑完整个分片，砍掉每患者 42 ms）；
③ 端云链路吞吐再提升（当前大载荷渐近 11.3 MB/s）。
按测算，三者齐备时 8 例档位可到持平附近（约 +9%），仍然是**擦线**——
这与 §13/§14 的结论一致：**串行/数据并行的诊断协同在本集群硬件上只能逼近持平**，
真正稳定的协同收益出现在端侧算力受限的工况（§14.5：端侧弱 6 倍时 +13.0%，弱 10 倍时 +51.4%）。

---

## 17. v3.5c：按算力加权分片 + 公平基线（诊断类）——收敛到持平

### 17.1 两个发现

1. **均分分片是错的**：数据中心 node3（4 核，与调度器/redis/患者库共处）实测每例完整模型约 230 ms，
   而医院 Pod 约 130 ms。**均分会让慢的那台决定墙钟**。改为按「每例成本」倒数加权分配
   （成本取各次实测的滚动平均），批量 2/4/8 的份额变为 2/0、3/1、6/2。
2. **本地批量并行反而更慢**：把本地批量改成按配额 4 路并发后，本地时延从
   批量 8 例 920 ms **恶化到 2117 ms**——4 路 × 每路 4 线程在 4 核配额上严重超订。
   因此**本地策略的最优实现是串行**，对比必须以它的最优为准（`LOCAL_BATCH_PARALLEL=1`，
   代码保留该开关并记录实测依据）。

### 17.2 脱站点对照（`test/diagnosis_ab.py`，两边各自最优）

| 批量 | 本地 mean（p50） | 协同 mean（p50） | 差值(mean) | 差值(p50) |
|---|---|---|---|---|
| 2 例 | 356（342） | 558（573） | −56.8% | −67.4% |
| 4 例 | 529（498） | 680（642） | −28.5% | −28.9% |
| **8 例** | 1 128（1 068） | **1 170（1 070）** | **−3.7%** | **−0.1%（持平）** |

### 17.3 结论

- **诊断类在端侧空闲时收敛到持平**（大批量下 p50 差 0.1%），但达不到 +10%：
  输入总量与本地相同（每个执行者只收到**自己的分片**，不是双份）；协同多出来的是
  **控制面往返**（下计划 + 回传记账，各约 40 ms）、**每请求固定开销**（实测建连/解析 42 ms，
  分片越小占比越高）与编排本身。这些开销随批量增大被摊薄，
  所以差距从批量 2 例的 −57% 收敛到 8 例的 −4%。
- **协同在诊断上的稳定收益出现在端侧算力受限工况**（§14.5：端侧弱 6 倍 +13.0%、弱 10 倍 +51.4%）。
- 这与 §13 的切分点分析互相印证：**诊断是"算力搬不动"的任务**——两端做的是同一份推理，
  协同只会增加搬运与编排；只有当端侧算力明显不足时，云端分担的那部分才值回票价。

### 17.4 若要继续推进（已量化，收益有限）

批量端点（一次请求跑完整个分片，省掉每患者 42 ms）、输入单份下发（执行者从患者库取数）、
端云链路提速。按当前数据，三者齐备时批量 8 例约可到 +8–12%（擦线），
**不可能出现"全面快 10%"**：本地路径只需一次调用、一份数据、零编排。
